# daily_brief — 2026-09-17

- 집계 기간: 2026-09-16 18:00 ~ 2026-09-17 21:27 (Asia/Seoul, 요청 시점)

## 발행 이슈

- 총 48건
- 완료 43건 · 진행 3건(SUU-127 진행 중, SUU-130 리뷰 중, SUU-128 예정) · 예정 1건(SUU-131) · 취소 1건(SUU-115)
- 이슈: SUU-83~112, SUU-114~131

## 오늘 한 일 요약

- **ECHO 파이프라인을 끝까지 연결했다.** ZIP 수집·무결성 검사·보관(SUU-94~98) → CSV 파싱·변환 규칙·11개 CSV 파싱(SUU-102~111) → 실제 ZIP 2개 파싱(SUU-112) → Supabase ECHO 테이블 14개(SUU-121) → release 등록·COPY 적재·검사·공개 포인터·한 번에 실행(SUU-122~126). 실제 첫 적재(SUU-127)는 진행 중. ADI·Dashboard 회신 PDF 수집(SUU-114), 공식 Subpart 사전(SUU-104).
- **RAG 검색을 vector / reranker / hybrid 세 조합으로 만들고 평가 체계를 다시 세웠다.** 색인 품질 수정(SUU-82~91), 검색 API·RRF·Kanon 2 리랭커·BM25(SUU-92, 99~101, 116), 채점표를 Hit@5·Hit@20·nDCG@10(주)·Recall@20으로(SUU-117), 평가셋 v1 32건 → **v2 102건**(SUU-120).
- **v2 102건 재측정(SUU-129)**: nDCG@10 vector 0.437 / reranker 0.561 / hybrid **0.568**. reranker→hybrid 98건 동일(+3/−1). 공통 실패 7건, 그중 4건은 정답이 Subpart A 일반 규정(63.8·63.9). 비용 Kanon $16.
- **후보 풀 진단 + RRF 가중치 스윕(SUU-130, $0, PR #105 리뷰 대기)**: 132조합 중 최고 recall@150 0.900 vs 기본 0.889 = 동점 → **가중치 튜닝 안 함**. hybrid 실패 7건 중 5건은 후보 안에 있는데 리랭커가 못 올린 rank-miss → **병목은 리랭커**. BM25 단독 풀(0.873) > vector 단독(0.842). adi-M200005는 후보 안에 있음(SUU-119 판단 정정).
- 워크플로: worktree 규칙(SUU-93), 파서 수정(SUU-118), 평가셋 초안 티켓 취소(SUU-115). 로컬 GPU 확인: RTX 4070 Laptop 8GB, torch는 CPU 전용이라 CUDA 재설치 필요.

## 내일 할 일

- **PR #105(SUU-130) merge** → main 최신화 → Linear SUU-130 Done.
- **SUU-131 bge reranker 비교(C-8)**: `pip install torch --index-url https://download.pytorch.org/whl/cu124` + `sentence-transformers`(≈2.5GB) → `bge-reranker-v2-m3`(≈2.2GB)로 reranker·hybrid를 v2 102건에 GPU로 돌려 Kanon과 같은 채점표로 비교. $0, 약 5분. nDCG@10 차이 0.03 미만이면 동점 → 무료 bge 채택 검토.
- **nDCG@10 개선 방향(합의된 순서, SUU-131 뒤 티켓화)**:
  1. **Subpart 먼저 찾고 그 안에서 조문 찾기 + Subpart A(63.1~63.16)는 항상 후보 포함** — Subpart Hit@5 0.88 ≫ 조문 Hit@5 0.80. 엉뚱한 Subpart 표·조문 제거, 63.8/63.9 pool-miss 해결. $0, 검색 코드 변경.
  2. **조문 단위 리랭크** — 청크 150개 대신 조문 30~50개(청크 합침)를 리랭크. 채점 단위와 일치, 정답 여러 개 케이스에 효과. $0.
  3. **LLM 리랭커** — 상위 30개를 Claude/GPT가 한 번에 줄 세움. 건당 ~1센트.
  4. 표(appendix-Table) → 부르는 조문으로 매핑 또는 순위 뒤로. 5. 질문 다시 쓰기(긴 ADI 서술 → 검색용 문장). 6. 리랭커가 나쁘게 만든 17건 라벨 점검. 7. 색인 변경은 마지막.
- 결정할 것: C-5 합격선·C-6 hybrid 채택은 C-8 뒤. ECHO 첫 적재(SUU-127)·파서 해시 수정(SUU-128)은 별도 흐름.
- 커밋 안 한 로컬 파일: `[1] docs/.../1_eCFR 구축 계획.md`(M), `echo_code_map.jsonl.bak`, `rag_eval_case_v2.draft.jsonl` — 처리 여부 확인.
