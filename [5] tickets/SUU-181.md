# SUU-181 chore(frontend): 히어로 은색 metal 글자를 없앤다

## 목표
첫 화면 h1이 은색 그라데이션(SUU-176) 대신 흰 글자(text-ink)다.

## 건드릴 파일
- `[4]frontend/src/app/page.tsx` — h1 `text-metal` → `text-ink`
- `[4]frontend/src/app/globals.css` — `@utility text-metal` 블록 삭제
- `[4]frontend/tests/topnav_hero.test.tsx` — "h1은 metal" 테스트를 "text-ink, text-metal 없음"으로
- `[4]frontend/tests/test_design_tokens.py` — text-metal 있음 검사 → 없음 검사

## 안 하는 것
- 설명문 `text-accent-blue`(SUU-176)는 유지
- h1 문구·nowrap·크기 변경 없음

## 완료 기준 ↔ 테스트
| 완료 기준 | 테스트 함수 |
|---|---|
| h1에 `text-ink` 있고 `text-metal` 없다 | `topnav_hero.test.tsx` > "h1은 흰 글자(text-ink)이고 metal·파란 강조는 없다" |
| globals.css에 `text-metal` 없다 | `test_design_tokens.py::test_globals_css_has_no_text_metal` |

## Codex 메모
Claude가 직접 구현함(사용자 요청). 되돌리기 성격이라 설계·구현을 한 커밋에.
