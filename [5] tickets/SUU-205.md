# SUU-205 fix(frontend): 랜딩에서 판화풍 미국 국기를 뺀다

## 목표
첫 화면(/) 우측 상단의 판화풍 미국 국기 그림을 없앤다. 공장 에칭 배경과 연기만 남는다.

## 건드릴 파일
- 고칠 것: `[4]frontend/src/app/page.tsx` — 국기 `<Image src="/flag-etching.png">` 블록 삭제
- 지울 것: `[4]frontend/public/flag-etching.png`
- 이미 한 것(Claude): `[4]frontend/tests/hero_flag_etching.test.tsx` 삭제 → `hero_no_flag.test.tsx`로 교체

## 안 하는 것
- h1·설명문·Start 버튼 되살리기 (SUU-203에서 없앤 상태 유지)
- 공장 배경 이미지·HeroSmoke 애니메이션 수정
- 상단바, 다른 페이지
- `tests/` 아래 수정

## 완료 기준 ↔ 테스트
| 완료 기준 | 테스트 함수 (`[4]frontend/tests/hero_no_flag.test.tsx`) |
|---|---|
| / 에 alt="Etched American flag" img가 없다 | `/ 에 alt가 Etched American flag 인 img가 없다` |
| public/flag-etching.png 파일이 없다 | `public/flag-etching.png 파일이 없다` |
| 공장 배경 img·연기 2개는 그대로 | `공장 에칭 img 와 hero-smoke 2개는 그대로 있다` |
| h1·설명문·Start는 여전히 없다 | `/ 에 h1·설명문·Start 링크는 여전히 없다` |

## Codex 메모
- `page.tsx`에서 지울 건 두 번째 `<Image>`(src="/flag-etching.png", 24~32행) 하나뿐. 첫 번째 `<Image src="/hero-etching.png">`, `<div bg-canvas/60>`, `<HeroSmoke>` 2개는 그대로 둔다
- `Image` import는 공장 배경이 아직 쓰므로 남긴다
- png는 `git rm "[4]frontend/public/flag-etching.png"`
- 확인: `[4]frontend`에서 `npm test` 전부 초록
