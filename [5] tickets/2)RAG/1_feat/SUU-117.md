# SUU-117 feat(rag): 평가 채점표를 Hit·nDCG@10·Recall로 바꾼다

## 목표
평가 채점표를 **Hit@5 · Hit@20 · nDCG@10(주) · Recall@20** 네 자로 고정하고, 조합 이름을 `vector` / `reranker` / `hybrid`로 통일한다. Claude가 직접 구현했다.

## 건드린 파일
- 고침: `[2] db/pipeline/5_rag/ecfr_eval.py` — `summarize`에 `ndcg` (이진 정답, k별)
- 고침: `[2] db/tests/5_rag/test_ecfr_eval.py` — `test_summarize_ndcg_binary_gain`
- 고침: `[6] rag/eval/run_eval.py` — `--config vector|reranker|hybrid`, 채점표 한 줄 출력
- 고침: `[6] rag/eval/rag_eval_plan.md` — [3]-3 채점표, [4] 조합 이름
- 추가: `[6] rag/eval/results/2026-09-17_vector_v1.jsonl`, `runs.jsonl` 한 줄 — 새 채점표 확인용 vector 실행(무료)

## 안 하는 것
- reranker·hybrid 실행(다음 티켓), 평가셋 변경, 검색 코드 변경, 지난 run의 config 이름 바꾸기

## 완료 기준 ↔ 테스트
| 완료 기준 | 테스트 함수 |
|---|---|
| nDCG@k = 케이스별 DCG/IDCG 평균, 정답 전부 1점, 없으면 0 | `test_summarize_ndcg_binary_gain` |
| `--config vector` 실행이 돈다 | 실행 결과 (아래) |

## 결과
vector, v1 32건: Hit@5 0.844 · Hit@20 0.969 · nDCG@10 0.573 · Recall@20 0.812 (MRR 0.618 = SUU-81 contextual과 동일)
