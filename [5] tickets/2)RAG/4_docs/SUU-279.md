# SUU-279 docs(rag): 답 모양을 나열로 최종 결정하고 기록

## 목표
계획 문서 `[1] docs/3) rag/2_rag 구축 계획.md`에 "답 모양은 나열(criteria)"이라는 최종 결정을 기록한다. 문서만 고친다. 코드 변경 없음.

## 건드릴 파일
- 고침: `[1] docs/3) rag/2_rag 구축 계획.md` — [0] 한 줄 요약, [6] 8단계(서비스 연결), [7] 1번·8번 줄, [9] 메모 한 줄
- 테스트(이미 고쳐 둠, 수정 금지): `[6] rag/tests/test_answer_plan_choices.py`

## 안 하는 것
- 코드 수정. `ecfr_answer.py`(`ANSWER_SYSTEM_GATES`·gates 파서·`verify_answer`), `run_answer.py`, `answer_pass_line.py`(`PASS_LINE`·`PASS_LINE_V2`), 채점기 전부 그대로. 관문 실험은 기록으로 남긴다
- `[6] rag/eval/answer_runs.jsonl`·`results/` 수정
- [7] 2~7번 줄·[10]·[11] 수정. 관문 run 숫자와 눈 검사 기록은 역사다
- `1_project.md`·`about-stats.json` 숫자 갱신(별도 티켓)
- 프론트·백엔드

## 완료 기준 ↔ 테스트
| 완료 기준 | 테스트 함수 |
|---|---|
| [7] 1번 줄에 "고른 것: 나열"이 있고, 나열 run(`2026-09-26_answer_criteria_gpt-5-mini_subset51`)과 관문 run(`2026-09-26_answer_gates_gpt-5-mini_subset51_out20k`) id가 둘 다 남아 있고, "( )"가 없다 | `test_stage_1_picked_criteria_and_keeps_both_runs` |
| [7] 8번 줄에 "모양 나열"·"gpt-5-mini"·`2026-09-19_answer_v2_gpt-5-mini_top10`이 있고 "모양 관문"이 없다 | `test_final_combination_is_criteria_on_the_102_case_run` |
| [0] 한 줄 요약과 [6] "8단계. 서비스 연결" 문단에 "나열"이 있고 "관문"이 없다 | `test_summary_and_service_step_describe_the_criteria_shape` |
| [7]은 여전히 8줄, 2·3·4·6번은 "건너뜀", 5·7번은 채워져 있고 7번에 "102건" | 기존 테스트 3개(그대로) |

## Codex 메모

### 왜
102건 전체에서 나열이 관문보다 낫다: Subpart 적중 0.961 vs 0.843, 인용 회수율 0.739 vs 0.610, 근거율 0.996 vs 0.892, 형식 실패 0 vs 11, 질문당 $0.015 vs $0.023, 중앙값 39초 vs 67초. [7] 1번 줄은 "G0 동점, G1 관문 승"으로 관문을 골랐지만 G1은 나열에 없는 칸이라 비교가 아니고, [6]-1 규칙대로 G3까지 내려가면 0.719 vs 0.619로 나열 승이다. 사용자가 2026-09-26에 나열로 결정했다. 서비스(`/ask`·화면·PDF)는 이미 나열이라 코드는 안 바꾼다.

### 고칠 곳 (줄 번호는 현재 문서 기준, 앞뒤 문맥으로 찾는다)

1. **[0] 한 줄 요약** (7행)
   - 지금: `… 후보 Subpart마다 "이 조건이면 적용"을 관문 순서로, 조문 인용과 원문 발췌를 달아 돌려준다. 판정은 하지 않는다.`
   - 바꿈: `… 후보 Subpart마다 "이 조건이면 적용"을 조문 인용과 함께 나열해 돌려준다. 판정은 하지 않는다.`
   - 이 문단에 "관문"이라는 낱말이 남으면 안 된다.

2. **[6] 10) 8단계. 서비스 연결** (약 199~204행)
   - 지움: `- 프론트: Subparts·Checklist 칸을 관문 구조로. 관문을 하나씩 스트리밍으로 받는다.`
   - 지움: `` - `rag_answer_log.answer`를 새 모양으로. ``
   - 넣음(첫 줄로, 마크다운 그대로): ``- 화면·`/ask`·`rag_answer_log.answer`는 나열 모양 그대로. 고칠 것 없음(1단계 재결정, [7]-1).``
   - `precedents[]`·`amendments[]` 두 줄은 그대로 둔다.
   - 이 문단(다음 빈 줄까지)에 "관문"이 남으면 안 된다. 테스트는 `"8단계. 서비스 연결"` 뒤부터 첫 빈 줄(`\n\n`)까지를 본다.

3. **[7] 1번 줄** (216행)
   - `고른 것: 관문` → `고른 것: 나열 (9/26 재결정)`
   - `이유:` 뒤를 이렇게: `G0 차이 0.032는 동점 폭 0.04 안. G1·G4는 나열에 없는 칸이라 비교 대상이 아니고, 규칙대로 G2(0.039, 동점)→G3(0.100, 폭 밖)까지 내려가면 나열 승. 102건에서도 나열이 G2 0.961 vs 0.843·G3 0.739 vs 0.610으로 앞선다. 처음엔 G1을 근거로 관문을 골랐다가 되돌렸다.`
   - 눈 10건 문장, `run: … vs …` 두 run id, `(SUU-277)` 문장은 **그대로 남긴다**. 관문 run id를 지우면 테스트가 빨강이다.

4. **[7] 8번 줄** (223행) 통째로 바꿈:
   `8) 최종 조합 한 줄: 모양 나열 + 조문 10개 + 덧붙임 없음 + 생성 한 번 + 검증문 없음(나열엔 발췌가 없어 검증할 것이 없음) + 모델 gpt-5-mini → 점수 102건 subpart 0.961 / citation_recall 0.739 / citation_grounded 0.996 / judge 1.775 → 합격선 PASS_LINE (subpart 0.93 / citation_recall 0.70 / citation_grounded 0.96 / judge 1.71). run: 2026-09-19_answer_v2_gpt-5-mini_top10. 관문 조합(검증문 있음)의 102건 점수 G0~G4 (0.892 / 0.892 / 0.843 / 0.610 / 0.892)·PASS_LINE_V2는 실험 기록으로 남긴다.`
   - 숫자 출처: `python "[6] rag/eval/answer_pass_line.py"` 출력의 v1 줄(`run: 2026-09-19_answer_v2_gpt-5-mini_top10`)과 `answer_pass_line.py`의 `PASS_LINE` 상수.

5. **[9] 정한 것과 이유** 끝에 한 줄 추가:
   `8) 관문 실험은 기록으로 남김(run·코드·[7]-1) → 되살리려면 파서 완화부터. 102건 형식 실패 11건은 빈 gates 후보 하나(6건)와 test가 yes/no 객체(5건)로, 모델 답이 아니라 포장 문제였다.`
   - [9] 2)·3)("관문 순서", "원문 발췌 필수")은 지우지 않고 각 줄 끝에 ` (1단계에서 나열로 재결정, [7]-1)`만 붙인다.

### 확인
- `[6] rag/tests`에서 `python -m pytest -q test_answer_plan_choices.py` 6개 초록. 루트에서 `python -m pytest -q`도 초록이어야 한다.
- 참고: 루트 작업 폴더에 사용자가 옮기는 중인 문서 삭제·미추적 파일이 있다. 이 티켓과 무관. 커밋에 넣지 않는다(`git add`는 계획 문서·설계 파일·테스트 파일만).
- 커밋: 계획 문서 1 + 설계 파일 + 테스트 파일. PR 본문에는 [7] 1번·8번 줄의 전후를 붙인다.
