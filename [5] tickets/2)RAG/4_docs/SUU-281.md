# SUU-281 docs(rag): 포트폴리오 문서의 주장을 실측에 맞게 고침

## 목표
`[1] docs/1) project/2_Portfolio.md`의 주장 중 저장소 실측과 다른 곳을 사실대로 고치고, 빠진 실험(임베더 비교)과 직접 판단 사례를 추가한다. 문서만 고친다.

## 건드릴 파일
- 고침: `[1] docs/1) project/2_Portfolio.md` — 9곳
- 테스트(Claude가 씀): `[6] rag/tests/test_portfolio_claims.py`

## 고친 것
1. "없는 근거를 구조적으로 막았다" → 판정 필드 제거만 구조적, 밖의 인용은 근거율 지표(0.996)로 잡음. 코드가 지우는 것은 다음 단계
2. "합격선 아래면 병합 차단"(3곳) → 평가를 돌린 뒤 기록된 점수가 선 아래면 차단. PR마다 재평가는 안 함(퇴보 방지선)
3. 검색 개선 0단계 추가: bge-m3 0.297 → Kanon 2 0.437 (+0.140). 리랭커 "두 번째", 규칙 "세 번째"로 순위 수정
4. 직접 정한 판단 5번 추가: Subpart RRR 오염 발견, Subpart A 고정 삽입 거부, 관문 형식 기각
5. 총 39초(리랭크 20초 포함)·질문당 약 $0.02로 합침
6. 데이터셋 번호 1,2,3,1 → 1,2,3,4 / PostgreDB → PostgreSQL / 배포 주소 추가

## 숫자 출처
- `[6] rag/eval/runs.jsonl`: `2026-09-18_vector_v2_bge`(0.297/0.490/0.676), `2026-09-17_vector_v2`(0.437/0.686/0.843)
- `[6] rag/eval/pass_line.py`, `[6] rag/tests/test_pass_line.py`: 기록된 run을 읽어 검사
- `[2] db/pipeline/5_rag/ecfr_answer.py` `parse_answer`: 밖의 인용은 지우지 않고 issues에만 기록

## 안 하는 것
- 코드·run·합격선 변경
- 밖의 인용을 코드가 지우게 하는 것 (별도 티켓)
- 작업 폴더의 다른 문서 이동·삭제는 이 PR에 넣지 않음

## 완료 기준 ↔ 테스트
| 완료 기준 | 테스트 함수 |
|---|---|
| 옛 문구("구조적으로 막았", "PostgreDB", "5단계 중 가장 크게")가 없다 | `test_old_overclaims_are_gone` |
| 임베더 0단계에 0.297과 0.437이 있다 | `test_embedder_stage_has_both_scores` |
| 합격선 문구에 "퇴보 방지선"이 2곳 이상 있다 | `test_pass_line_wording_is_honest` |
| 직접 판단 5번에 사례 3개가 있다 | `test_own_judgement_section_lists_three_cases` |

## 메모
- 이 티켓은 Claude가 직접 구현한다(사용자 지시). Codex 작업 없음.
