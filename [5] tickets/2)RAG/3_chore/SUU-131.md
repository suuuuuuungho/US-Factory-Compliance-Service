# SUU-131 chore(rag): bge·nemotron reranker를 재서 Kanon과 비교한다

## 목표
Kanon 2 대신 무료 로컬 모델 두 개(`BAAI/bge-reranker-v2-m3`, `nvidia/llama-nemotron-rerank-1b-v2`)를 RTX 4070에서 돌려 reranker·hybrid를 v2 102건에 재고, Kanon(SUU-129)과 같은 채점표로 비교한다. $0. C-8.

## 건드린 파일
- 만듦: `[2] db/pipeline/5_rag/ecfr_rerank_local.py` — `rerank_pairs`(Kanon과 같은 본문), `build_local_reranker`(배치 32), `load_scorer("bge"|"nemotron")`(torch·모델은 늦게 import)
- 고침: `[6] rag/eval/run_eval.py` — `--reranker kanon|bge|nemotron`(기본 kanon), `run_id_for`(로컬이면 `_bge` 등 붙임), `rerank_model`(runs.jsonl 필드), 로컬은 비용 0
- 만듦: `[2] db/tests/5_rag/test_ecfr_rerank_local.py`, `[6] rag/tests/test_run_eval_reranker.py`
- 만듦: `results/2026-09-18_{reranker,hybrid}_v2_{bge,nemotron}.jsonl`, `runs.jsonl` 4줄
- 고침: `results/rag_eval_result.md`(SUU-131 절 맨 앞), `rag_eval_plan.md` [14] C-8, `requirements.txt`(로컬 리랭커 설치 메모)

## 안 하는 것
- 검색 기본 리랭커 변경, 세 모델 외 추가 비교, RRF 가중치 변경, 합격선(C-5), hybrid 채택(C-6)
- CI에 torch·모델 설치(requirements.txt엔 메모만)

## 완료 기준 ↔ 테스트
| 완료 기준 | 테스트 함수 |
|---|---|
| 로컬 리랭커가 Kanon과 같은 본문(context + chunk)을 쌍으로 만든다 | `test_local_pairs_match_kanon_texts` |
| 청크 수만큼 점수, 모델 호출은 배치 단위 | `test_local_rerank_returns_score_per_chunk` |
| 모르는 이름이면 바로 에러 | `test_unknown_reranker_raises` |
| `runs.jsonl`에 `rerank_model` 기록 | `test_runs_has_rerank_model` |
| Kanon run_id는 그대로, 로컬만 접미사 | `test_run_id_keeps_kanon_name_and_adds_local_suffix` |

## 결과
- hybrid nDCG@10: **Kanon 0.568 > nemotron 0.512 > bge 0.485** > vector 0.437. reranker 조합도 같은 순서(0.561 / 0.509 / 0.485).
- top-20 실패: Kanon 7 → nemotron 12 → bge 17. bge는 vector가 맞히던 6건을 20등 밖으로 밀었다.
- 세 리랭커 공통 실패 5건(M100004·M110005·M180004·M200005·Z060002), 그중 4건이 Subpart A 정답 → 모델 교체로는 못 잡는다.
- **Kanon 2 유지.** 로컬이 꼭 필요하면 nemotron(케이스당 리랭크 6~8초).

## 다음
C-5 합격선·C-6 hybrid 채택. nDCG 개선 티켓: Subpart 우선 + Subpart A 항상 포함 → 조문 단위 리랭크 → LLM 리랭커.
