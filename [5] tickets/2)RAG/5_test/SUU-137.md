# SUU-137 test(rag): 검색 합격선을 정하고 최신 run을 CI가 검사한다

## 목표
C-5. 기본 조합(hybrid + 규칙 + LLM gpt-5-mini) 점수에서 동점 폭 0.03을 뺀 퇴보 방지선을 정하고, `runs.jsonl`의 기본 조합 최신 run을 테스트가 검사해 CI가 매 PR마다 확인한다. $0.

## 합격선 (v2 102건)
| 지표 | 합격선 | 지금 |
|---|---|---|
| nDCG@10 | ≥ 0.66 | 0.692 |
| Hit@20 (top-20 실패율 ≤ 5%) | ≥ 0.95 | 0.961 |
| Subpart 적중률@20 | ≥ 0.95 | 0.980 |

## 건드린 파일
- 만듦: `[6] rag/eval/pass_line.py` — `DEFAULT_RUN`, `PASS_LINE`, `latest_default_run`, `check_pass_line`, `main`(불합격이면 exit 1)
- 고침: `[6] rag/eval/llm_rerank.py` — `--replay`(저장된 답 재사용, $0), `run_record`로 `runs.jsonl`에 run 한 줄 기록
- 고침: `[6] rag/eval/runs.jsonl` — `2026-09-18_hybrid_v2_llm_gpt-5-mini` 추가(리플레이)
- 만듦: `[6] rag/tests/test_pass_line.py`
- 고침: `rag_eval_plan.md` [13]·[14] C-5·C-6, `results/rag_eval_result.md`, `[1] docs/3) rag/2_rag 구축 계획.md` [10] 4)·5), `3_rag 검색 품질 개선 과정.md`

## 안 하는 것
- Kanon·OpenAI 재실행, CI 워크플로 변경(pytest가 이미 돈다), 합격선 상향·지표 추가

## 완료 기준 ↔ 테스트
| 완료 기준 | 테스트 함수 |
|---|---|
| 기본 조합(hybrid·v2·규칙·gpt-5-mini) run 중 가장 최근 것을 고른다. 없으면 None | `test_latest_default_run_picks_newest_of_default_combo` |
| 합격선 아래 지표만 실패로 표시 | `test_check_pass_line_flags_metrics_below_line` |
| 실제 `runs.jsonl` 최신 기본 조합 run이 합격선 통과 | `test_latest_default_run_in_runs_jsonl_passes` |

## 결과
- `python "[6] rag/eval/pass_line.py"` → OK 0.692 / 0.961 / 0.980. 전체 테스트 290개 통과.
- 앞으로 기본 조합을 다시 재면(`run_eval.py --llm gpt-5-mini` 또는 `llm_rerank.py`) runs.jsonl에 붙는 최신 run을 CI가 검사한다.

## 다음
답변 생성 단계(상위 5조문 → 답). Subpart A 후보 강제.
