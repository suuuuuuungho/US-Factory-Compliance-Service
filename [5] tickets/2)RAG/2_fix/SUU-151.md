# SUU-151 fix(rag): 정답 Subpart가 여럿이면 A 빼고 하나만 맞아도 적중으로 센다

## 목표
`score_subpart`가 `gold_subparts[0]`만 보던 것을 "A를 뺀 정답 중 하나라도 후보에 있으면 1"로 바꾸고, 저장된 두 run을 `--replay`($0)로 다시 채점한다.

## 건드릴 파일
- 고침: `[2] db/pipeline/5_rag/ecfr_answer_score.py` `score_subpart` (시그니처 그대로)
- 실행: `run_answer.py --replay --run-id …` 두 번(baseline, prompt2) → `answer_runs.jsonl` 두 줄 갱신(줄 수 유지, notes에 "SUU-151 replay" 덧붙임), `answer_failures.py` 표 두 번
- 문서: `[1] docs/3) rag/4_rag 답변 품질 개선 과정.md` 2절 Subpart 적중 정의·5절 점수판·실패 분류, `[6] rag/eval/rag_eval_plan.md` [14], `results/rag_eval_result.md` SUU-151 절
- 테스트(이미 있음, 수정 금지): `[2] db/tests/5_rag/test_ecfr_answer_score.py` 끝 3개

## 안 하는 것
- 평가셋(`gold_subparts`) 수정, 프롬프트·모델 변경, OpenAI 호출
- `answer_failures.py` 변경 — `out["subpart"]`만 보므로 자동으로 따라온다

## 완료 기준 ↔ 테스트
| 완료 기준 | 테스트 함수 |
|---|---|
| `[ZZZZ, PPPPP]`에 `PPPPP` → 1 | `test_score_subpart_hits_when_any_non_a_gold_is_present` |
| `[FFFFF, A]`에 `A`만 → 0, `FFFFF` → 1 | `test_score_subpart_does_not_count_general_provisions_a_alone` |
| `[A]`뿐이면 `A` → 1 | `test_score_subpart_counts_a_when_it_is_the_only_gold` |
| replay: baseline 0.912→0.941, prompt2 0.922→0.951, 문서 갱신 | 사람 확인 |

## Codex 메모
- `gold = set(case["gold_subparts"]); real = gold - {"A"} or gold` → `int(bool(real & candidates))`. 후보는 이미 `normalize_subpart` 거침.
- replay는 `PYTHONIOENCODING=utf-8`로. `answer_runs.jsonl`은 append되므로 옛 두 줄을 지우고 새 두 줄만 남긴다(cost_usd는 옛 줄 값 복사, notes 이어 붙임).
- 미리 계산: 바뀌는 케이스는 두 run 모두 eaton-auburn, adi-M170004 2건(→ `answer` 태그에서 빠짐). adi-M080034(A만)는 그대로 0.

## 결과 (2026-09-19, Claude 구현)
- 테스트 335 초록. replay $0.
- baseline Subpart 0.912 → **0.931**(실패 10 → 9), 프롬프트 v2 0.922 → 0.941(실패 11 → 9). 설계 때 적은 "0.941/0.951"은 A까지 세는 계산이었고, A를 빼면 바뀌는 건 두 run 모두 2건(eaton-auburn, adi-M170004).
