# SUU-192 fix(frontend): 히어로 보라 배경을 빼고 다른 페이지와 같은 배경으로

## 목표
`/` 히어로에서 `bg-gradient-violet`을 빼서 다른 페이지처럼 기본 canvas 배경이 되게 한다.

## 건드릴 파일
- `[4]frontend/src/app/page.tsx` — section className에서 `bg-gradient-violet` 제거

## 안 하는 것
- 제목·부제·Start 버튼·레이아웃 변경, `tests/` 수정

## 완료 기준 ↔ 테스트
| 완료 기준 | 테스트 함수 |
|---|---|
| region "Hero"에 bg-gradient-violet 없음 | `hero_landing.test.tsx` — `히어로 섹션(region=Hero)에 gradient 배경 클래스가 없다` |
| h1·Start 링크 그대로 | `hero_landing.test.tsx` 나머지 2개 (기존) |

## Codex 메모
- 클래스 하나만 지우면 된다. 나중에 이미지가 오면 그 자리에 배경 이미지를 넣는다.
