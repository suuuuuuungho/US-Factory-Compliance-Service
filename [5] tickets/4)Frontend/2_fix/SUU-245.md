# SUU-245 fix(frontend): ECHO deviation 설명을 타일 아래로, 타일 가운데 정렬

## 목표
- "What is a Title V deviation?" 상자를 표 아래에서 **타일 5개 바로 아래**로
- 타일 글자 가운데 정렬(`text-center`)
- 총액 타일 라벨에서 `(9,998 actions)` 제거

## 건드릴 파일
- `[4]frontend/src/app/echo/page.tsx`
- `[4]frontend/tests/echo_dashboard.test.tsx`

## 완료 기준 ↔ 테스트
| 완료 기준 | 테스트 |
|---|---|
| 타일 그리드의 다음 형제가 deviation-note, 표는 그 뒤 | `Title V deviation 설명은 타일 5개 바로 아래, 표보다 위에 있다` |
| 타일에 `text-center`, 라벨에 `actions` 없음 | `타일 글자는 가운데 정렬, 총액 라벨에 건수 괄호 없음` |
