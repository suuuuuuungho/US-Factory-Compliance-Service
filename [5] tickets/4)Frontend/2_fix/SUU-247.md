# SUU-247 fix(frontend): Ask 뒤 Workspace가 폭 0으로 안 보이던 것 고침

## 목표
- 배포 주소에서 질문을 보낸 뒤 검은 화면에 세로선 하나만 보이던 것을 고친다
- 원인: SUU-215가 부모 div에 `items-center`를 넣어 Workspace 상자 폭이 0으로 줄었다

## 건드릴 파일
- `[4]frontend/src/app/Workspace.tsx` — 상자에 `w-full`
- `[4]frontend/tests/workspace.test.tsx`

## 완료 기준 ↔ 테스트
| 완료 기준 | 테스트 |
|---|---|
| dockview 상자 className에 `w-full` | `dockview 상자는 w-full이다 (items-center 부모 안에서 폭 0 방지)` |
