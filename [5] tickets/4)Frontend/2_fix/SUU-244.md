# SUU-244 fix(frontend): FR 페이지 버튼 영어로, 표 바로 아래에 붙임

## 목표
SUU-243 의 이전/다음 버튼을 `Prev` / `Next` 로. 목록이 `flex-1` 이라 페이지 바가 화면 맨 아래로 밀려 표와 너무 멀다 → 표 바로 아래 `mt-3` 여백으로 붙인다. 목록은 넘칠 때만 스크롤.

## 건드릴 파일
- 고칠 것 `[4]frontend/src/app/federal-register/page.tsx`

## 안 하는 것
- 페이지 크기·diff 화면 변경

## 완료 기준 ↔ 테스트
테스트 파일: `[4]frontend/tests/fr_list_pagination.test.tsx` (SUU-243 테스트를 고친다)

| 완료 기준 | 테스트 함수 |
|---|---|
| 버튼 이름 `Prev` / `Next` | 기존 테스트의 버튼 이름 |
| list pane 에 `flex-1` 없음, 페이지 바(`[data-pager]`)에 `mt-3` | `페이지 바는 표 바로 아래에 붙는다` |

## Codex 메모
Claude가 직접 구현·merge (사용자 지시).
