# SUU-243 fix(frontend): FR 목록 스크롤 되고 10개씩 페이지 넘김

## 목표
`/federal-register` 목록(43개)이 `main`의 `md:overflow-hidden` 때문에 잘리고 스크롤이 안 된다. 목록 영역이 남는 높이 안에서 스크롤되고, 한 페이지 10개씩 이전/다음으로 넘긴다.

## 건드릴 파일
- 고칠 것 `[4]frontend/src/app/federal-register/page.tsx` — 목록 pane `overflow-y-auto` + 페이지 상태

## 안 하는 것
- diff 화면 변경
- URL 에 페이지 번호 넣기

## 완료 기준 ↔ 테스트
테스트 파일: `[4]frontend/tests/fr_list_pagination.test.tsx`

| 완료 기준 | 테스트 함수 |
|---|---|
| `[data-pane='list']` 에 `overflow-y-auto` 와 `min-h-0` | `목록 영역은 스크롤된다` |
| 문서 23개면 첫 페이지 행 10개, `1 / 3` 표시, 이전 비활성 | `한 페이지 10개, 페이지 표시` |
| 다음 클릭 → 11~20번, 두 번 더 → 21~23 세 개, 다음 비활성 | `다음·이전으로 페이지를 넘긴다` |

## Codex 메모
Claude가 직접 구현·merge (사용자 지시).
