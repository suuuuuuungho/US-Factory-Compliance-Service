# SUU-284 feat(frontend): Applicability 예시 질문 대신 작성 안내 표시

## 목표
eCFR Applicability 화면의 긴 예시 질문 3개를 없애고, PromptBar 아래에 "질문을 이렇게 쓰세요" 안내 4줄을 보여준다.

## 건드릴 파일
- 고칠 것: `[4]frontend/src/app/applicability/page.tsx` — `EXAMPLES` 목록·`preset` 상태·PromptBar `value` prop 제거. PromptBar 아래 `<section aria-label="How to write your question">` 안에 `HOW_TO` 4줄(Where·Size·Process·Question)을 `<ol>`로 그린다
- 테스트: `[4]frontend/tests/example_questions.test.tsx` — 예시 3개 클릭 테스트를 안내 4줄 테스트로 교체

## 안 하는 것
- `src/components/PromptBar.tsx` 수정
- Ask 뒤 화면(Workspace·카드·PDF) 변경
- 다른 페이지·문서

## 완료 기준 ↔ 테스트
| 완료 기준 | 테스트 함수 |
|---|---|
| "Example questions" 목록이 없고 안내 영역에 항목이 정확히 4개 | `예시 질문 목록은 없고, PromptBar 아래에 작성 안내 4줄(Where·Size·Process·Question)이 있다` |
| 제목 28px·PromptBar 640px·가운데 정렬·안내문 그대로 | 같은 파일의 나머지 3개 테스트 |
| Send 동작 그대로 | `prompt_bar_ask.test.tsx`, `ask_inline_form.test.tsx` |

## Codex 메모
- 이 티켓은 설계와 구현을 Claude가 한 번에 했다. Codex 작업 없음
- 안내 문구는 테스트의 `textContent` 배열과 글자까지 같아야 한다 (`Where — State, and what the plant makes` 등)
