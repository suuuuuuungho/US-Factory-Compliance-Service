# SUU-217 feat(frontend): About 페이지에 서비스 소개·범위·출처를 쓴다

## 목표
빈 `/about`를 채운다. 무엇을 하고/안 하는지, 어떻게 돌아가는지, 데이터 출처, 면책. 제목·숫자·인용구는 New York(`font-serif`), 본문은 SF Pro(기본).

## 건드린 파일
- 고침 `[4]frontend/src/app/about/page.tsx` — 섹션 7개 (hero + Who / Why(4) / Do·Don't(5·2)+인용구 / How(3) / Sources(4) / Disclaimer)
- 만듦 `[4]frontend/tests/about_page.test.tsx`, 고침 `tests/empty_pages.test.tsx` (About 제외), `tests/example_questions.test.tsx` (Applicability h1 기준을 About 대신 클래스 문자열로)

## 안 하는 것
- 폰트 추가 없음. SF Pro(기본 sans)·New York(`font-serif`)은 SUU-204/211에서 이미 들어옴
- 다른 페이지

## 완료 기준 ↔ 테스트
| 완료 기준 | 테스트 |
|---|---|
| h1에 font-serif | `about_page.test.tsx` 1 |
| h2 6개 순서 | 2 |
| do 5 / don't 2 | 3 |
| 출처 eCFR·Federal Register·ECHO·ADI | 4 |
| "not legal advice" 면책 | 5 |
| Applicability/Amendment/Community 빈 페이지 그대로 | `empty_pages.test.tsx` |

## 메모
- Claude가 직접 구현 (Codex 단계 생략).
- untracked `src/lib/utils.ts`(`export { cn } from "cn"`)는 이 PR과 무관하며 커밋하지 않음. 남아 있으면 로컬 `next build`가 실패한다.
