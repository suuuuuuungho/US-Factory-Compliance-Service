# SUU-248 feat(frontend): ECHO 주별 벌금을 미국 지도로 보여준다

## 목표
SUU-246 의 "Top 10 states by penalty dollars" 가로 막대를 **미국 지도(choropleth)** 로 바꾼다. 벌금 총액이 큰 주일수록 밝고, 마우스를 올리면 아래에 `TX · $144.8M · 2,538 facilities`.

## 건드릴 파일
- 새로 `[4]frontend/src/components/UsStateMap.tsx` — `us-atlas/states-10m.json` → `topojson-client.feature` → `@visx/geo` `AlbersUsa`(975×610 viewBox, scale 1300). FIPS→우편약자 표 51개. 밝기 = `0.12 + 0.78·√(값/최대)` 흰색 알파. 데이터 없는 주 0.06
- `[4]frontend/src/app/echo/page.tsx` — `top-states` HBar 자리에 `data-chart="state-map"` + `UsStateMap`
- `[4]frontend/package.json` — `@visx/geo`, `topojson-client`, `us-atlas`, dev `@types/topojson-client`, `@types/geojson`
- 테스트 `[4]frontend/tests/echo_dashboard.test.tsx`

## 안 하는 것
- `echo-stats.json`·파이프라인 수정 (states[] 는 SUU-246 그대로)
- 카운티 지도, 확대/축소

## 완료 기준 ↔ 테스트
| 완료 기준 | 테스트 |
|---|---|
| `[data-chart='state-map']` 안에 svg | `차트 5개가 모두 SVG 로 그려진다` |
| 주 path 50개 이상, NM > TX > MI 밝기, 데이터 없는 주(WY) 가장 어둡다, hover 텍스트 | `주별 지도: 주 path 50개 이상, NM 이 TX 보다 밝고, hover 하면 이름·금액` |

## 실제로 한 것 (Claude, 2026-09-22)
- vitest 137 통과, lint 0 에러, build 통과, headless Chromium 스크린샷으로 지도·hover 확인
