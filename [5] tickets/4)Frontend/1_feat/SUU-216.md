# SUU-216 feat(frontend): Send 뒤 대기 화면에 안내문과 진행 단계

## 목표
Send 를 누르면 답이 올 때까지 "Please wait a moment" 안내문과, 지금 무엇을 하는 중인지 단계 문구가 보인다. 단계는 서버가 알려주지 않으므로 시간에 따라 바뀐다.

## 건드릴 파일
- 만들 것 `[4]frontend/src/app/Waiting.tsx` — `export const STEPS = [{at:0,…},{at:6,…},{at:14,…},{at:25,…}]`, `role="status"` 안에 안내문 + 현재 단계. 1초마다 지난 시간을 세서 단계를 고른다
- 고칠 것 `[4]frontend/src/app/applicability/page.tsx` — `loading && <p role="status">Searching…</p>` → `loading && <Waiting />`

## 안 하는 것
- 서버 스트리밍/실제 진행률, Stop 버튼 동작, 답 뒤 화면, 에러 표시(그대로)

## 완료 기준 ↔ 테스트
| 완료 기준 | 테스트 함수 (`waiting_screen.test.tsx`) |
|---|---|
| Send 뒤 role=status 에 안내문 + 첫 단계 | `Send 뒤 role=status 안에 'Please wait a moment' 와 첫 단계 문구가 있다` |
| 단계가 0/6/14/25초에 바뀜 | `단계 문구가 0초·6초·14초·25초에 차례로 바뀐다` |

## 메모
- 단계 문구: Searching 40 CFR Part 63 for rules that match your process… / Reading the candidate subparts… / Writing the checklist and citations… / Almost there — the server is taking a little longer than usual…
- 기존 `page.test.tsx` 의 "로딩 표시(role=status)" 검사는 role 만 보므로 그대로 통과
- Ask 뒤 Question 칸 안(status)에서도 같은 컴포넌트가 보인다
