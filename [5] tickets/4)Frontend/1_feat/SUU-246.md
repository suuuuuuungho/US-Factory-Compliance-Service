# SUU-246 feat(frontend): ECHO 차트 4개 추가(연도별 $·Subpart·주·벌금 분포)

## 목표
`/echo` 에 차트 4개를 더해 5개를 2열 그리드로 보여준다.

| 차트 | 모양 | 데이터 |
|---|---|---|
| Violations and penalty actions by year | 세로 막대 (기존) | `yearly[].violations / penalties` |
| Penalty dollars by year | 세로 막대, 툴팁 `$150.3M` | `yearly[].penalty_usd` (새로) |
| Top 10 Subparts by penalty dollars | 가로 막대 (`BarYAxis` 로 왼쪽에 코드) | `subparts[]` 에서 클라이언트가 정렬·10개 |
| Top 10 states by penalty dollars | 가로 막대 | `states[]` (새로) |
| How big is one penalty? | 세로 막대 5구간 | `penalty_buckets[]` (새로) |

## 건드릴 파일
- `[2] db/pipeline/3_ECHO/echo_stats.py` — `echo_facility` 조회에 `state` 추가. `yearly[].penalty_usd`(그 해 벌금 합), `states[]`(주별 `_stats_for`, 벌금 총액 큰 순), `penalty_buckets[]`(`<$1K / $1K–10K / $10K–100K / $100K–1M / $1M+`, 2015~ 처분)
- `[4]frontend/public/echo-stats.json` — 재생성 (release a1d0ad6d). 주 55개, 구간 585 / 4,612 / 3,831 / 867 / 103
- `[4]frontend/src/app/echo/page.tsx` — `ChartCard`, `HBar`(horizontal BarChart + BarYAxis, margin.left 64), `top10()`. `data-chart="yearly | yearly-usd | top-subparts | top-states | buckets"`
- 테스트 `[2] db/tests/3_ECHO/test_echo_stats.py`, `[4]frontend/tests/echo_dashboard.test.tsx`

## 안 하는 것
- 지도(US map) — topojson·지도 라이브러리가 더 필요해서 가로 막대로 대신
- `src/components/charts/**` 수정

## 완료 기준 ↔ 테스트
| 완료 기준 | 테스트 |
|---|---|
| TX(F1·F2)·LA(F3) 로 묶이고 벌금 총액 큰 순 | `test_states_group_facilities_by_state_sorted_by_penalty_total` |
| $500 / $12,000 / $2,500,000 → 구간 1·0·1·0·1, yearly penalty_usd | `test_penalty_buckets_count_each_action_once` |
| 빈 release → states [], 구간 전부 0 | `test_empty_release_gives_zero_and_null_not_division_error` |
| 차트 5개 모두 `<svg>` | `차트 5개가 모두 SVG 로 그려진다` |
| 가로 막대 10개, 총액 큰 순, 왼쪽 라벨 | `가로 막대는 벌금 총액 큰 순 10개, 왼쪽에 이름이 보인다` |
| 구간 라벨 5개 | `벌금 크기 분포 차트는 구간 5개 라벨을 찍는다` |

## 실제로 한 것 (Claude, 2026-09-22)
- pytest echo_stats 9 통과, vitest 135 통과, lint 0 에러, headless Chromium 스크린샷으로 5개 차트 배치 확인
