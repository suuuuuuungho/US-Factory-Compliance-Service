# SUU-144 chore(frontend): 프론트를 비우고 다시 시작한다

## 목표
프론트를 처음부터 다시 만들기 위해 `[4]frontend`를 비우고, CI frontend job과 Vercel 프로젝트를 없앤다.

## 건드린 파일
- 삭제: `[4]frontend/` 전부 (Next.js 코드·설정·테스트·판화 그림) → `.gitkeep`만
- 고침: `.github/workflows/ci.yml` — `frontend` job 제거
- 고침: `[1] docs/4) workflow/1_workflow.md` — 7번 표 6행 메모
- Vercel 프로젝트 `lg-cns/us-factory-compliance` 삭제 (CLI)

## 안 하는 것
- `[5] tickets/SUU-140~143.md` (기록), Linear 이슈, 다른 폴더

## 완료 기준 ↔ 확인
| 완료 기준 | 확인 |
|---|---|
| `[4]frontend/`에 `.gitkeep`만 | `git ls-files "[4]frontend"` |
| ci.yml에 `frontend:` 없음 | grep |
| Vercel 프로젝트 없음 | `vercel project ls` |

## 되살리기
- 코드: `git checkout f5aa448 -- "[4]frontend"` (SUU-142 merge 시점)
- 판화 그림 6장: 같은 커밋의 `[4]frontend/public/engraving/`
- 배운 것: Vercel Root Directory에 공백 불가, `[4]`는 setup-node cache 경로에서 `[[]4]`로 이스케이프, jsdom에선 `import.meta.url`이 http:
