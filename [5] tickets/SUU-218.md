# SUU-218 feat(frontend): 메뉴를 Federal Register·EPA ECHO·Decision Letter로

## 목표
상단바 메뉴가 About / Applicability / Federal Register / EPA ECHO / EPA Decision Letter 5개가 되고, Community는 사라진다.

## 건드릴 파일
- 고칠 것 `[4]frontend/src/app/TopNav.tsx` — `MENU`를 5개로: `["Federal Register","/federal-register"]`, `["EPA ECHO","/echo"]`, `["EPA Decision Letter","/decision-letter"]`. Community 제거
- 이름변경 `[4]frontend/src/app/amendment/` → `federal-register/` (h1: `Federal Register`, 함수명 `FederalRegisterPage`)
- 만들 것 `[4]frontend/src/app/echo/page.tsx` (h1: `EPA ECHO`), `decision-letter/page.tsx` (h1: `EPA Decision Letter`) — `about/page.tsx`와 같은 모양
- 지울 것 `[4]frontend/src/app/community/`

## 안 하는 것
- 페이지 내용 채우기, 메뉴 스타일 변경, `tests/` 수정

## 완료 기준 ↔ 테스트
| 완료 기준 | 테스트 함수 (`nav_menu.test.tsx`) |
|---|---|
| 링크 5개 순서·href | `TopNav에 링크 5개가 순서대로 있고 href가 맞다` |
| 새 페이지 3개 h1 | `/federal-register, /echo, /decision-letter 페이지 h1이 맞다` |
| Community·amendment 없음 | `Community 링크와 /community, /amendment 폴더가 없다` |

## Codex 메모
- 기존 테스트 `empty_pages`, `topnav_menu`, `topnav_transparent`도 새 메뉴 이름으로 이미 고쳐 둠. 함께 초록이어야 한다
- 빈 페이지 모양은 `[4]frontend/src/app/about/page.tsx` 그대로 (`main.flex-1.px-6.py-10` > `h1.text-[28px]`)
- 폴더는 `git mv`로 옮기고 `community`는 `git rm -r`
