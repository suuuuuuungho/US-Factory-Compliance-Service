# SUU-169 chore(backend): 답변 LLM을 다시 gpt-5-mini로 되돌린다

## 목표
SUU-168은 OpenAI 크레딧이 없는 동안 답을 보려고 임시로 Claude Haiku 4.5를 쓴 것. 확인이 끝났으니 PR #143을 revert해서 OpenAI(gpt-5-mini)로 되돌린다.

## 건드릴 파일
- `git revert e014d8c` — `[3] backend/app/claude.py` 삭제, `main.py`·`answer.py`·`requirements.txt`·테스트 2개 원복
- `[5] tickets/SUU-168.md`는 기록으로 남긴다

## 안 하는 것
- 코드 새로 고치기 — revert만

## 완료 기준 ↔ 테스트
| 완료 기준 | 테스트 함수 |
|---|---|
| 답 요청 모델이 `gpt-5-mini`, 단가 원복 | `test_answer.py` 기존 테스트 |
| `requirements.txt`에 anthropic 없음 | `test_no_claude_left_in_pipeline_or_requirements` |
| Render `OPENAI_API_KEY` 교체 후 화면에서 답이 나온다 | 사람 확인 |
