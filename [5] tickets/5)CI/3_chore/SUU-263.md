# SUU-263 chore(ci): 티켓 설계 파일을 영역·종류 폴더로 나눈다

## 목표
`[5] tickets` 바로 아래 218개 설계 파일을 `[5] tickets/<영역>/<종류>/SUU-N.md`로 나누고, CI·명령·문서가 새 경로를 쓰게 한다.

## 건드릴 파일
- 이동: `[5] tickets/SUU-*.md` → 제목의 `종류(영역)` 기준 폴더 (test(rag)는 `2)RAG/5_test`)
- `.github/workflows/ci.yml`: 설계 파일을 `[5] tickets` 아래 전체에서 `rglob`으로 찾는다
- `[1] docs/4) workflow/2_rules.md`: 12절 "설계 파일 위치" 추가, 경로 예시 수정
- `.claude/commands/ticket.md`, `.claude/commands/spec.md`, `AGENTS.md`, `.github/pull_request_template.md`, `[1] docs/4) workflow/1_workflow.md`: 새 경로
- 옛 경로 참조: `[3] backend/app/letters.py` 주석, `[6] rag/eval/rag_eval_plan.md`, `[1] docs/2) db/db 구축 계획/4_ADI+CAA 구축 계획.md`

## 안 하는 것
- 티켓 파일 안에 적힌 옛 경로(기록이라 그대로 둔다)
- 빈 폴더용 `.gitkeep` (파일이 생길 때 폴더도 생긴다)

## 완료 기준
- `[5] tickets` 바로 아래에 `SUU-*.md`가 없다
- 이 PR에서 CI 규칙 검사(설계 파일 첫 줄 = PR 제목)가 통과한다
