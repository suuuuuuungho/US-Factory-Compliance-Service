# SUU-227 docs(ci): worktree 규칙을 CLAUDE.md·2_rules.md에 적는다

## 목표
"새 터미널은 무조건 `claude --worktree <이름>`으로 연다" 규칙을 main에 올린다.

## 건드릴 파일
- 고칠 것: `CLAUDE.md` (worktree 한 줄 추가), `[1] docs/4) workflow/2_rules.md` 3절 (worktree 규칙 확장) — 이미 수정된 내용 그대로 커밋
- 만들 것: 이 설계 파일

## 안 하는 것
- 코드, 테스트 (티켓이 문서만 다룬다고 명시)
- 다른 문서·hook 파일 수정

## 완료 기준 ↔ 테스트
| 완료 기준 | 테스트 함수 |
|---|---|
| main의 `2_rules.md` 3절에 worktree 규칙이 있다 | 없음 (문서 티켓, 사람이 PR diff로 확인) |
| main의 `CLAUDE.md`에 "새 터미널은 worktree" 한 줄이 있다 | 없음 (문서 티켓, 사람이 PR diff로 확인) |

## Codex 메모
- 문서 변경과 설계 파일은 이 브랜치에 이미 커밋·push 되어 있다. Codex는 PR만 만든다.
- PR 제목: `docs(ci): worktree 규칙을 CLAUDE.md·2_rules.md에 적는다 (SUU-227)`
