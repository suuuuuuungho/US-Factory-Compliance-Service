# SUU-234 fix(frontend): FR 목록에서 Section 열을 뺀다

## 목표
SUU-232에서 넣은 Section(조항 번호) 열이 너무 길어 표를 밀어내므로 뺀다. 머리글은 `#`, `Subpart`, `Title`, `Effective`, `Changes` 다섯 개.

## 건드릴 파일
- 고칠 것 `[4]frontend/src/app/federal-register/page.tsx` — `<th>Section</th>`와 해당 `<td>` 한 줄씩 삭제

## 안 하는 것
- `FrDiff.tsx`·JSON·파이프라인 수정 (조항 번호는 diff 영역 `§ 63.xxx` 제목에서 본다)
- 다른 열·레이아웃 변경

## 완료 기준 ↔ 테스트
테스트 파일: `[4]frontend/tests/fr_list_view.test.tsx`

| 완료 기준 | 테스트 함수 |
|---|---|
| 머리글 5개 순서 | `표 머리글: #·Subpart·Title·Effective·Changes` |
| 칸이 5개, 시행일은 4번째 칸, `amended_sections` 값은 표에 없음 | `첫 행: # 1, subpart 'A, T', 개정일은 시행일, 조항 번호는 표에 없다` |
| diff 없는 문서도 칸 순서 동일 | `diff 없는 문서는 # 2, subpart '—', 개정일 '—', 행이 회색(text-ink-muted)` |

## Codex 메모
Claude가 직접 구현·merge (사용자 지시). 두 줄 삭제 + 주석 갱신뿐.
