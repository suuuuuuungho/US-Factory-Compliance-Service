# SUU-168 feat(backend): 답변 LLM을 임시로 claude-haiku-4-5로 바꾼다

## 목표
OpenAI 크레딧이 0이라 Render `/ask`가 500으로 죽는다(`credit_balance_exhausted`). 화면에서 답이 어떻게 나오는지 보려고 LLM 두 단계(리랭크·최종 답)를 임시로 Claude Haiku 4.5로 돌린다. 검색(Kanon)은 그대로. 확인 뒤 OpenAI로 되돌린다.

## 건드릴 파일
- 추가: `[3] backend/app/claude.py` — `claude-haiku-4-5` 호출. 반환 모양은 `call_openai_rerank_api`/`call_openai_chat`과 같음
- 고침: `[3] backend/app/main.py` — Claude 함수를 쓴다
- 고침: `[3] backend/app/answer.py` — 답 요청 모델·단가($1/$5)
- 고침: `requirements.txt` — `anthropic==1.7.0`
- 테스트: `[3] backend/tests/test_answer.py` 모델·단가, `[2] db/tests/5_rag/test_ecfr_chunk_index.py` anthropic 금지 검사에서 `requirements.txt`만 임시 제외

## 안 하는 것
- `[2] db/pipeline/5_rag/*` 수정 — 평가 파이프라인은 OpenAI 그대로
- Render 환경변수 — `ANTHROPIC_API_KEY`는 사람이 대시보드에서 넣는다

## 완료 기준 ↔ 테스트
| 완료 기준 | 테스트 함수 |
|---|---|
| 답 요청 모델이 `claude-haiku-4-5`, 비용이 Haiku 단가 | `test_answer.py` 기존 테스트 |
| Vercel 화면에서 질문 → 답이 나온다 | 사람 확인 → 티켓 댓글 |

## 되돌릴 때
이 PR을 revert하고 Render `OPENAI_API_KEY`에 크레딧 있는 키를 넣는다.
