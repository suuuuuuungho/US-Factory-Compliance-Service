# SUU-251 feat(frontend): ECHO 페이지 강조색·지도 금액별 보라 램프

## 목표
회색뿐이던 `/echo` 에 **색 역할**을 정해 준다. 지도는 금액이 클수록 진하게.

| 역할 | 색 | 어디에 |
|---|---|---|
| 벌금 $ | 보라 `gradient-violet` `#6a4cf5` (Applicability 카드와 같은 브랜드 색) | 연도별 $ 선, Subpart 가로 막대, 벌금 크기 막대, 정렬 중인 헤더, 체크박스 |
| 위반 | 코랄 `gradient-coral` `#ff5577` | 연도별 위반 선 |
| 타일 5개 숫자 | ink (색 없음) | 맨 위 카드 전부 |

지도 램프(`rampColor`): 연보라 `#e6e1ff` → 보라 `#6a4cf5` → 짙은 보라 `#3418b8`. 사이는 `color-mix(in oklch)`. 단계는 √(값/최대). 데이터 없는 주는 `#1c1c1c`. 아래 범례 막대 `Less ▬▬▬ $191.8M`.

## 건드릴 파일
- `[4]frontend/src/components/UsStateMap.tsx` — `NONE`/`RAMP`/`rampColor`, path 에 `data-level`, hover 시 `brightness-125`, 범례 + hover 한 줄
- `[4]frontend/src/components/YearLineChart.tsx` — 선 아래 같은 색 `AreaClosed` 그라디언트(0.28 → 0)
- `[4]frontend/src/app/echo/page.tsx` — `Tile` 에 `tone`, 선·막대 색, 정렬 헤더·체크박스 보라, 지도 설명 "darker = more", 표를 `overflow-x-auto` 상자로
- 테스트 `[4]frontend/tests/echo_dashboard.test.tsx`

## 안 하는 것
- `globals.css` 토큰 추가 (있는 토큰만 씀), `src/components/charts/**` 수정
- TopNav 가 폰에서 451px 로 넘치는 것 (페이지 밖, 별도 티켓)

## 완료 기준 ↔ 테스트
| 완료 기준 | 테스트 |
|---|---|
| NM level 1 = `#3418b8`, TX 는 color-mix, WY(데이터 없음) `#1c1c1c`, 범례에 최대값, hover | `주별 지도: … 금액 클수록 진하게 …` |
| 타일 보라/코랄/ink, 선 stroke 보라/코랄, 그라디언트 2개, 정렬 헤더 보라, 표 overflow-x-auto | `색 역할: …` |

## 실제로 한 것 (Claude, 2026-09-22)
- 처음엔 "클수록 밝게"로 했다가 요청대로 "클수록 진하게"로 램프 뒤집음
- prettier 가 page.tsx 전체를 다시 줄바꿈해서(324줄 diff) 되돌리고 필요한 줄만 다시 고침 (최종 diff 22+/16-)
- vitest 139 통과, lint 0 에러, build 통과, headless Chromium 데스크톱·폰(390px) 확인

## 후속 (같은 티켓, 2차 PR)
- deviation 설명 안의 `deviation`·`Title V deviation %` 는 색 빼고 `font-semibold` 만
- 맨 위 타일 5개 숫자도 색 빼고 기존대로 흰색 (`Tile` 의 `tone` 제거)
