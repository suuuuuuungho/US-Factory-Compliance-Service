# SUU-99 feat(db): rag_chunk 키워드 검색용 tsv를 채우고 SQL 함수를 만든다

## 목표
`rag_chunk.tsv`를 전부 채우고, 질문 문자열로 키워드 점수 상위 k개 청크를 돌려주는 `rag_keyword_search`를 만든다. hybrid(SUU-100)가 부른다.

## 건드릴 파일
- 만들 것: `[2] db/migrations/SUU-99_rag_keyword_search.sql` (Supabase에 적용 완료, 2026-09-17)

## 안 하는 것
- Python 코드, RRF 합치기, `index_chunk`에서 tsv 자동 채우기(다음 재색인 때)

## 완료 기준 ↔ 확인
| 완료 기준 | 확인 |
|---|---|
| `count(tsv)` = 5,625 | SQL로 확인 ✅ |
| 평가셋 32개 질문이 오류 없이 돌고 지연 기록 | 32/32 성공, p50 1,025ms / p95 1,818ms (RPC 왕복 포함) |
| 키워드만으로 정답 조문이 잡히는가(참고) | 조문 Hit@5 15/32, Hit@20 22/32, 상위 150청크 안 27/32 |

## 결정한 것
- 질문 단어를 **OR**로 잇는다. `websearch_to_tsquery`(AND)는 긴 질문에서 0건.
- `ts_rank_cd` 정규화 **2(길이로 나눔)**. 0/1/2 비교: Hit@20 17/17/22, MRR 0.20/0.24/0.39.
- 티켓에 적었던 "'cathode slurry'로 63.11607이 나온다"는 기준은 틀렸다. 그 조문에는 그 단어가 없다(F3 어휘 불일치는 키워드로도 안 잡힘). 이 케이스는 리랭커/확장 몫.
