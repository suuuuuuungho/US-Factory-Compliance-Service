# SUU-239 fix(frontend): FR diff 상자를 Applicability 카드 디자인으로 맞춘다

## 목표
- diff 스크롤 상자를 Applicability 카드와 같은 모양으로: `rounded-lg`, 보라(`bg-gradient-violet`) sticky 머리글, 22px 굵은 제목
- "Federal Register" 제목 가운데 정렬. 뒤로가기 아이콘은 제목과 같은 줄(같은 y)
- 문서 인용·제목은 sticky 머리글 안으로 넣어 diff 상자가 위로 올라온다. 상자는 화면 남는 높이를 다 쓴다 (`md:h-[calc(100dvh-60px)]`, Applicability 와 같음)

## 건드릴 파일
- 고칠 것 `[4]frontend/src/app/federal-register/page.tsx`
- 고칠 것 `[4]frontend/src/components/FrDiff.tsx`

## 안 하는 것
- diff 계산·목록 표 내용 변경

## 완료 기준 ↔ 테스트
테스트 파일: `[4]frontend/tests/fr_diff_scroll.test.tsx` 에 추가.

| 완료 기준 | 테스트 함수 |
|---|---|
| 제목 h1 이 `text-center`, 뒤로 버튼과 h1 의 부모가 같다, 스크롤 상자 `rounded-lg`, 머리글 `bg-gradient-violet` + `sticky` | `Applicability 카드와 같은 디자인, 제목 가운데, 뒤로 버튼은 제목과 같은 줄` |

## Codex 메모
Claude가 직접 구현·merge (사용자 지시).
- 클래스는 `applicability/page.tsx` 의 `CARD`/`CARD_TITLE` 값을 그대로 옮긴다
