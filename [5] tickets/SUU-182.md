# SUU-182 feat(frontend): 화면을 질문 줄 + 3열(Subpart·Checklist·조문)로 나눈다

## 목표
1행은 질문 폼, 2행은 Subparts | Checklist | 조문 세 칸. 칸마다 따로 스크롤.

## 건드릴 파일
- `[4]frontend/src/app/page.tsx` — JSX 구조·className만

## 안 하는 것
- API·기능·문구·카드 스타일 변경 (제목 띠 통일은 SUU-183)
- `layout.tsx`, `globals.css`, `api.ts`

## 완료 기준 ↔ 테스트
| 완료 기준 | 테스트 함수 |
|---|---|
| grid 컨테이너 `grid md:grid-cols-3`, form `md:col-span-3` | `three_columns.test.tsx` > "결과 영역은 3열 grid이고 질문 폼이 3열을 다 차지한다" |
| Subpart 카드는 "Subparts" region, Checklist는 "Checklist" region 안 | "Subpart 카드는 Subparts 칸에, Checklist는 Checklist 칸에 있다" |
| 3열: 클릭 전 안내 문구, 클릭 후 `<aside>` | "3열은 인용 클릭 전엔 안내 문구, 클릭 후엔 조문 패널이다" |
| 세 칸 모두 `overflow-y-auto` | "2행 세 칸 모두 따로 스크롤된다" |

## Codex 메모
Claude가 직접 구현. 바깥 `<div className="flex flex-1">` + `<main max-w-3xl>` + 바깥 `<aside>` 구조를 `<main max-w-7xl>` 하나 + 안쪽 grid로 바꿈. `COLUMN = "flex flex-col gap-6 overflow-y-auto md:max-h-[calc(100vh-22rem)]"`. 2행은 `result.answer`가 있을 때만 그림. `section.test.tsx`의 "클릭 전 complementary 없음"은 그대로 통과.
