# SUU-183 feat(frontend): 2행 세 칸 제목 띠를 같은 모양·같은 보라색으로

## 목표
Subpart 카드·Checklist·조문 칸의 h2가 같은 className(위치·글꼴·크기·보라 배경)이다.

## 건드릴 파일
- `[4]frontend/src/app/page.tsx` — `BANDS` 순환 삭제, `CARD_TITLE`에 `bg-gradient-violet` 포함, 조문 `<aside>`를 `CARD`+`CARD_TITLE`로
- `[4]frontend/tests/card_title_band.test.tsx` — "2번째 magenta" → "전부 violet" + 조문 h2 className 동일 검사

## 안 하는 것
- 레이아웃(SUU-182), 기능, API, 문구

## 완료 기준 ↔ 테스트
| 완료 기준 | 테스트 함수 |
|---|---|
| 모든 h2 `bg-gradient-violet`, magenta 없음 | `card_title_band.test.tsx` > "모든 카드 제목 줄은 같은 보라색이다" |
| 조문 h2 className == Subpart h2 className | "조문 칸 제목 줄은 Subpart 제목 줄과 className이 같다" |

## Codex 메모
Claude가 직접 구현. SUU-182 브랜치 위에서 작업 (PR은 182 merge 뒤 main으로).
