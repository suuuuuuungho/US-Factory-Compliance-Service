# SUU-153 feat(rag): 답에 Subpart A 조문 3개를 항상 넣어 102건 다시 잰다

## 목표
`run_answer.py`에 `--always-a`를 붙여 상위 N 뒤에 공통 규칙 조문 3개(63.2 정의, 63.7 성능시험, 63.8 모니터링)를 항상 붙이고, 상위 10 + 항상 A로 102건 새 run(≈$3). A-5 ③.

## 건드릴 파일
- 고침: `[6] rag/eval/run_answer.py`
  - `ALWAYS_SECTIONS = ("section-63.2", "section-63.7", "section-63.8")`
  - `top_sections(line, top_n=TOP_N, always=())`: 상위 N 뒤에 `always` 중 아직 없는 것만 순서대로 붙임, subpart는 `"A"`
  - `--always-a` 플래그 → `always = ALWAYS_SECTIONS if args.always_a else ()`. `fetch_texts`·`one()` 둘 다 같은 `always`를 씀
  - `answer_run_record(..., always=())` → 기록에 `"always_sections": list(always)`
- 실행: `python "[6] rag/eval/run_answer.py" --top-n 10 --always-a --run-id 2026-09-20_answer_v2_gpt-5-mini_top10_alwaysA` → `results/…_top10_alwaysA.jsonl`, `answer_runs.jsonl` 한 줄 추가(4줄), `answer_failures.py --run-id …`
- 문서: `[1] docs/3) rag/4_rag 답변 품질 개선 과정.md` 4절 A-5 ③ 상태·5절 점수판 행 + 실패 분류·6절, `[6] rag/eval/rag_eval_plan.md` [14], `results/rag_eval_result.md` SUU-153 절
- 테스트(이미 있음, 수정 금지): `[6] rag/tests/test_run_answer.py` 끝 3개

## 안 하는 것
- 프롬프트(`ANSWER_SYSTEM`)·모델·심판·채점기·`answer_failures.py` 변경
- 63.6/63.9/63.10 추가, 조문 20개, `MAX_COMPLETION_TOKENS` 변경
- `TOP_N`·기본 동작 변경(플래그 없으면 SUU-152와 같아야 함)

## 완료 기준 ↔ 테스트
| 완료 기준 | 테스트 함수 |
|---|---|
| `always=ALWAYS_SECTIONS`면 상위 N 뒤에 63.2/63.7/63.8이 붙고, 이미 상위 N에 있으면 중복 없음, subpart는 A | `test_always_sections_are_appended_after_top_n_without_duplicates` |
| `always` 없으면 SUU-152와 동일 | `test_without_always_top_sections_is_unchanged` |
| run 기록에 `always_sections` | `test_answer_run_record_keeps_always_sections` |
| 새 run: 인용 Recall ≥ 0.77(0.739+0.03), Subpart·근거율 -0.03 이내, 심판 -0.06 이내 | 측정 → 문서, 사람 확인 |

## Codex 메모
- 비교 기준은 SUU-152 run(`2026-09-19_answer_v2_gpt-5-mini_top10`): 0.961 / 0.739 / 0.996 / 1.775, 실패 5.
- 조사($0, SUU-152 결과로): 정답 인용에 A 조문이 있는 케이스 29건, 그중 22건은 상위 10에 그 조문이 없음. 빈도 63.8 ×11, 63.2 ×6, 63.7 ×6. 모델이 안 준 A 조문을 인용한 지어냄 4건(63.8 ×2, 63.9, 63.10). 세 조문을 항상 주면 Recall 상한 0.793.
- 세 조문 크기: 63.2 2.8만자, 63.7 3.5만자, 63.8 3.0만자 → 입력 토큰 ≈ 27k → 50k/건, 비용 ≈ $3. 심판이 긴 입력에 흔들리는지 본다.
- `section_texts.json` 캐시에 63.2/63.7/63.8 이미 있음(다시 안 받음).
- `--always-a` 없이 돌리면 `answer_run_record`의 `always_sections`는 `[]`.
- `answer_runs.jsonl` notes: "SUU-153 상위 10 + Subpart A 63.2/63.7/63.8 항상(프롬프트 v2)". run이 append하니 줄 수(4줄)를 확인.

## 결과 (2026-09-20, Claude 구현)
- `--top-n 10 --always-a`: **0.971 / 0.785 / 1.000 / 1.755**, 실패 5→3, 지어냄 0, $2.27(빈 답 0건). 입력 토큰 44k/건.
- 기준 Recall ≥ 0.77 **통과**(+0.046). 심판 -0.02는 동점 폭 안(오른 8건·내린 10건, 0점 0건).
- 실패 3(검색 1 / 답 2)은 모두 검색이 정답 Subpart 조문을 못 올린 것. 살아난 2건 mosaic(지어냄 사라짐)·adi-M110015.
- 테스트 340 초록.
