# SUU-261 chore(ci): 안 쓰는 파일·폴더·브랜치 정리

## 무엇을

레포에 남은 임시 파일·빌드 결과물·큰 설치 파일과, 머지가 끝난 worktree·브랜치를 지운다.

## 배경

Codex 테스트 임시 폴더, 빌드 결과물, 폰트 설치 파일(218MB), eCFR 캐시(20MB), 안 쓰는 이미지·컴포넌트가 쌓였다. worktree 13개와 로컬 브랜치 51개도 남아 있었다.

## 범위

- Codex 테스트 임시 폴더(`.codex-*`, `tmp/`), `.pytest_cache`, `.next`, `tsconfig.tsbuildinfo` 삭제
- `SF-Pro.dmg`, `NY.dmg`, `default.png`, `smoke-3`, 안 쓰는 `SpringCheck.jsx/.css` 삭제
- ECHO 찌꺼기(`.zip.part`, `.jsonl.bak`, 옛 로그) 삭제
- RAG 초안(`rag_eval_case_v2.draft.jsonl`, `manual_test_questions.md`) 삭제
- eCFR 섹션 XML 캐시를 `.gitignore`에 추가 — `fr_diff.py`가 다시 받는다
- `Hero Image.png`를 `[4]frontend`로 이동
- 머지 끝난 worktree 13개, 로컬 브랜치 51개 제거
- 안 하는 것: 코드 동작 변경 없음

## 완료 기준

- [ ] 위 파일·폴더가 레포에 없다
- [ ] `git worktree list`에 `main` 하나만 남는다
- [ ] 기존 pytest / vitest가 그대로 통과한다
- [ ] 지운 브랜치의 내용이 모두 `origin/main`에 들어 있다 (작업 손실 없음)
