# SUU-154 chore(rag): 답변 run에 토큰·비용을 남기고 상위 10을 기준으로 정한다

## 목표
`answer_runs.jsonl` 네 줄 모두에 입력·출력 토큰을 남기고, 문서에 "기준 조합 = 상위 10(프롬프트 v2), 항상 A는 보류"를 적는다. $0.

## 건드릴 파일
- 고침: `[6] rag/eval/run_answer.py` — `answer_run_record(..., tokens: dict)` → 기록에 `"tokens": {"prompt", "completion", "prompt_per_case"}`. `main()`이 `usage`를 넘긴다
- 고침: `[6] rag/eval/answer_runs.jsonl` — 4줄 모두 `tokens` 채움(아래 표). 다른 값은 안 건드림
- 고침: `[1] docs/3) rag/4_rag 답변 품질 개선 과정.md` — 4절 A-5 ③을 "보류"로, 5절 점수판에 입력 토큰/건 열, 5절 아래 "기준 조합" 한 줄, 6절 배운 것 1줄
- 고침: `[6] rag/eval/rag_eval_plan.md` [14] A-5 ③ 행, `results/rag_eval_result.md` SUU-154 절(토큰·비용 표)
- 테스트(이미 있음, 수정 금지): `[6] rag/tests/test_run_answer.py` 끝 2개

## 안 하는 것
- OpenAI 호출, 새 run
- `--always-a` 코드·테스트·결과 파일 삭제(기록으로 남긴다)
- 합격선·CI — SUU-155

## 완료 기준 ↔ 테스트
| 완료 기준 | 테스트 함수 |
|---|---|
| `answer_run_record(tokens=usage)` → `tokens.prompt/completion/prompt_per_case`(prompt ÷ 케이스 수, 정수) | `test_answer_run_record_keeps_tokens` |
| `answer_runs.jsonl` 모든 줄에 `tokens` 세 값이 0보다 큼 | `test_every_saved_answer_run_has_tokens` |
| 문서에 기준 조합·보류 이유·토큰 열 | 사람 확인 |

## Codex 메모
- `usage`는 `main()`에 이미 있다(`{"prompt_tokens", "completion_tokens"}`). `prompt_per_case = prompt // len(outs)`.
- 네 run의 토큰(실행 로그에서, retry-empty 포함 합계):

| run_id | prompt | completion | prompt/건 | 비용 |
|---|---|---|---|---|
| 2026-09-19_answer_v2_gpt-5-mini (baseline, 상위 5) | 1,580,311 | 524,696 | 15,493 | $1.45 |
| …_prompt2 (프롬프트 v2, 상위 5) | 1,537,324 | 496,620 | 15,072 | $1.38 |
| …_top10 (상위 10) | 2,728,955 | 554,670 | 26,754 | $1.79 |
| 2026-09-20_…_top10_alwaysA (상위 10 + 항상 A) | 4,438,364 | 579,912 | 43,513 | $2.27 |

- 항상 A 보류 이유(문서에 그대로): 점수는 올랐지만(Recall 0.739→0.785) 검색 결과와 상관없이 특정 조문을 고정으로 끼워 넣는 건 "정답을 미리 알고 맞추는" 셈이라 서비스 방식으로 맞는지 확신이 없다. 비용도 $1.79→$2.27, 입력 토큰 27k→44k/건. 기록만 남기고 기준 조합은 상위 10.
