# SUU-127 feat(db): ECHO 2026-09-17 release를 실제로 처음 적재한다

## 목표
SUU-112가 만든 `parsed/2026-09-17/`을 진짜 Supabase에 넣고 검사·공개해서 DB에서 ECHO 시설·위반·처분을 조회할 수 있게 한다. 계획 3-3·3-4를 실제 수치로 채운다.

## 선행 조건 (전부 머지됨)
- SUU-121(표) · 122(등록) · 123(COPY) · 124(검사) · 125(공개) · 126(배선/CLI). 실행은 Claude가 한다 (SUU-98·112와 같은 실행 티켓).
- `.env`에 `SUPABASE_DB_URL` — Supabase 대시보드 → Connect → **Session pooler**(5432) 주소. 사람이 넣는다.

## 건드릴 파일
- `python "[2] db/pipeline/3_ECHO/echo_ingest.py" 2026-09-17`을 돌린다.
- 고칠 것: `[1] docs/2) db/db 구축 계획/3_ECHO 구축 계획.md` 3-3·3-4, 이 파일 하단 "실행 결과".
- `[2] db/pipeline/3_ECHO/echo_load.py` 2줄 — 실행 중 만난 문제라 fix 티켓을 따로 만들지 않고 이 PR에 넣는다 (사용자 결정):
  - `set statement_timeout = 0` — pooler 기본 2분 제한에 COPY가 끊김
  - `set session_replication_role = replica` — 행마다 FK 검사 트리거가 돌아 10배 느림. 고아 FK는 `echo_check`가 적재 뒤 검사
- 테스트: `[2] db/tests/3_ECHO/test_echo_load.py::test_session_drops_statement_timeout_and_fk_triggers_before_any_write`

## 안 하는 것
- 조회 뷰·인덱스 튜닝, 충돌 행 고치기

## 완료 기준 ↔ 확인 방법
| 완료 기준 | 확인 방법 |
|---|---|
| `common_dataset_current(echo, icis_air_national+caa_pipeline)`가 새 release를 가리키고 status가 published | Supabase SQL |
| 13개 표 행 수 = 2-5절 표 (`echo_facility` 280,071 …, `echo_source_row`는 held 0) | `check_release` 결과 + SQL count |
| 검사 결과·소요 시간·DB 용량 증가가 문서 3-4에 있다 | 계획 문서 |

## 실행 메모
- 실행 전 `python -m pytest -k echo` 초록 확인. 환경변수는 `.env`에서 읽어 넘긴다 (스크래치 런처, 코드 변경 아님).
- DB 용량은 실행 전후 `pg_database_size` / 표별 `pg_total_relation_size`로 잰다.

## 실행 결과
- 1차(2026-09-21 19:28): `echo_facility` COPY에서 statement timeout(2분) → `set statement_timeout = 0` 추가.
- 2차(19:38): 초당 약 2,000행, 18분에 `echo_pollutant`까지 → 중단. FK 트리거가 원인(FK 없는 임시 표는 초당 25,000행) → `set session_replication_role = replica` 추가.
- 3차(20:01:11 → 20:08:25, **7분 14초**): 검사 통과, published. release `a1d0ad6d-57c0-436b-ac7c-c90c64daccb8`, `common_dataset_current(echo)`가 가리킴.
- 12개 표 행 수 = 2-5절 jsonl 행 수 (facility 280,071 · activity 3,242,036 · activity_facility 3,246,696 · violation 102,676 …). `echo_source_row` 0(held 없음), `echo_code_map` 306.
- Part 63 후보: Subpart 행 64,955 / 시설 49,618 (2-5절과 같음).
- DB 용량 728 MB → 3,109 MB (+2.4 GB). echo_* 표·인덱스 2,884 MB.
- 자세한 것은 계획 문서 3-3·3-4.
