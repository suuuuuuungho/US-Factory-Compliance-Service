# SUU-112 feat(db): ECHO 실제 ZIP 2개를 처음 파싱한다

## 목표
SUU-98이 받아둔 진짜 ZIP 2개(2026-09-17)로 `parse_release`를 돌려 jsonl 13개와 `report.json`을 만들고, 수치를 계획 문서 2-3·2-4·2-5에 적는다.

## 선행 조건 (전부 머지됨)
- SUU-102~111 파서 모듈. 실행은 Claude가 한다 (SUU-98과 같은 실행 티켓).

## 건드릴 파일
- 새 코드 없음. 스크래치 스크립트로 `echo_codemap`·`echo_parse`를 부르기만 한다.
- 만들 것 (데이터): `[2] db/3) ECHO/code_map/2026-09-17/echo_code_map.jsonl` — 저장소에 커밋 (작다, `.gitignore` 대상 아님). `parsed/2026-09-17/`는 `.gitignore`가 막는다.
- 고칠 것: `[1] docs/2) db/db 구축 계획/3_ECHO 구축 계획.md` 2-3·2-4·2-5, 이 파일 하단 "실행 결과".

## 안 하는 것
- 코드·테스트 수정 (문제 있으면 fix 티켓)
- DB 적재 (Step 3), 충돌 행 고치기

## 완료 기준 ↔ 확인 방법
| 완료 기준 | 확인 방법 |
|---|---|
| `parsed/2026-09-17/` 아래 jsonl 13개 + `report.json` | 폴더 목록 |
| 파일별 `read = ok + held`가 원본 7,513,497 + 67,123행을 다 설명 | `report.files` 합계 ↔ manifest `row_count` |
| 고아 FK·중복·충돌·결측률·소요 시간이 문서에 있다 | 계획 문서 2-4·2-5 |

## 실행 메모
- 사전 → code_map → 파싱 순서. `parse_release(root, as_of, version)`의 root는 raw/와 code_map/이 같이 있는 `[2] db/3) ECHO` (본 저장소 폴더, worktree 아님).
- `echo_parse.py`에 CLI가 없어 스크래치에서 import해 부른다. 코드 변경 아님.

## 실행 결과
- 1차 실행(전날): Pipeline에서 행마다 집합을 다시 합쳐 초당 13행 → 메모리 부족으로 중단. fix SUU-118(PR #94) 후 재실행.
- 2차 실행: 391.8초. jsonl 13개 + `report.json` 생성. 총 6.9 GB (`echo_source_row` 4.56 GB).
- 11개 파일 모두 `read = ok + held`, 보류 0. read 합계 7,513,497 + 67,123 = manifest `row_count`와 파일마다 일치.
- 고아 FK 10개 0. 벌금 106,520 = FORMAL ok 106,520. Subpart mapped 155,395 / unresolved 35,831. Pipeline resolved 58,676 / unresolved 8,447 / 가짜 42,480.
- 충돌: `echo_activity` 50,869, `echo_pollutant` 113,062(그중 25,526은 행 번호만 다른 셈법 문제), `echo_program` 1.
- 수치와 원인은 계획 문서 2-3·2-4·2-5에 적었다.
- 후속 fix 후보: 내용 해시에서 `source_locator` 제외 (pollutant 중복이 충돌로 잡힘).
