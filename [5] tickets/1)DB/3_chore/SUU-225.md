# SUU-225 chore(db): ADI·Dashboard 회신을 실제로 파싱·적재한다

## 목표
SUU-221·222·224가 만든 `adi_parse → adi_enrich → adi_ingest`를 2026-09-17 실수집 원본으로 한 번 돌려 DB에서 ADI 4,061건 목록과 회신 701건을 조회할 수 있게 하고, 계획 문서 3-4절의 "미실행"을 실제 수치로 바꾼다.

## 선행 조건 (전부 머지됨)
- SUU-114(수집) · 221(목록·PDF 파서) · 222(블록·인용·관계·시설) · 223(표) · 224(등록·적재·검사·공개). 실행은 Claude가 한다 (SUU-98·112·127·210과 같은 실행 티켓).
- `common_dataset_current`에 `ecfr`·`echo` release가 공개돼 있어야 한다 (적재기가 복합 FK를 채운다). 실행 전 확인: ecfr `0c2efcae`, echo `a1d0ad6d`.
- `.env`의 `SUPABASE_URL`·`SUPABASE_SECRET_KEY`·`SUPABASE_DB_URL`.

## 건드릴 파일
- 실행 (원본·parsed는 메인 저장소 `[2] db/4) ADI+CAA/`, git 제외):
  1. `adi_parse.py --root … --as-of 2026-09-22 --raw-as-of 2026-09-17`
  2. `adi_enrich.py --root … --as-of 2026-09-22 --ecfr-nodes "[2] db/1) eCFR/parsed/2026-09-11/v1/nodes.jsonl" --echo-facilities "[2] db/3) ECHO/parsed/2026-09-17/echo_facility.jsonl"`
  3. `run_adi_ingest(root, "2026-09-22", client=…, conn=…)` (스크래치 런처, `adi_ingest.__main__`과 같은 일. worktree에는 데이터 폴더가 없어 root만 바꿨다)
- 고칠 것: `[1] docs/2) db/db 구축 계획/4_ADI+CAA 구축 계획.md` 머리말·2-4·2-5·3-3·3-4, 이 파일 하단 "실행 결과".
- 실행 중 만난 문제 2건은 fix 티켓을 따로 만들지 않고 이 PR에 넣는다 (사용자 결정, SUU-210과 같은 방식):
  - `adi_parse.py`·`adi_release.py`: 수집 때 ADI PDF 5건(0500031·0500041·0600010·0600070·0600085)이 `adi_letters/raw`와 `shard_*`에 두 번 받아져 manifest에 같은 파일이 2번 있다. 그대로 두면 `adi_entry_document`·`common_release_object` PK 충돌. 같은 (source_key/URL, sha256)는 한 번만 센다.
    - 테스트: `test_adi_parse.py::test_same_pdf_collected_twice_under_one_control_number_is_linked_once`, `test_adi_release.py::test_same_file_in_two_manifests_is_one_raw_object_and_one_link`
  - `adi_load.py`: `adi_cfr_reference` COPY 열에 `historical_*`·`current_*` 4개가 두 번 들어가 실 DB가 `column "historical_ecfr_release_id" specified more than once`로 거부. jsonl 키에서 그 4개를 빼고 뒤에 한 번만 붙인다. SUU-224의 가짜 DB는 열 이름 중복을 안 잡아서 통과했었다.
    - 테스트: `test_adi_load.py::test_copy_column_lists_have_no_duplicate_names`

## 안 하는 것
- 위 2건 외 코드·테스트 수정, OCR(글자 없는 버전 10건), RAG 청크 생성, 자동 갱신(Step 4), 2005년 이전 ADI 스캔 PDF 536건 수집

## 완료 기준 ↔ 확인 방법
| 완료 기준 | 확인 방법 |
|---|---|
| `adi_source_entry` 4,061행, `common_dataset_current(adi)` 존재 | `select count(*) from adi_source_entry` = 4,061 · `common_dataset_current` where dataset='adi' → 1행 |
| `adi_document_version` ≥ 698, 페이지 없는 버전 0 | count = 701 · `version not exists page` = 0 |
| quality_report의 held 건수와 사유가 계획서에 기록됨 | 계획 문서 2-4절 (held 5: pypdf 오류) |

## 실행 결과
- 파싱(11:49, **72초**): 목록 4,061행(ADI 3,825 + Dashboard 236). PDF 706개(ADI 477 + Dashboard 229; manifest 항목은 711, 중복 5). 성공 4,056 + held 5. 행: source_entry 4,061 · document 701 · document_version 701 · entry_document 701 · page 2,333(ok 2,292 · empty 41).
  - held 5 (전부 ADI, pypdf 예외): M960008 `list indices must be integers or slices, not str`, 0100025·0200082·0400028·0400032 `AssertionError`. 목록에는 `text_status=text_failed`로 남는다.
  - `text_status`: ADI ok 472 · text_failed 5 · text_missing 3,348(2005년 이전 미수집 등) / Dashboard ok 229 · text_missing 7(링크 없음 6 + 404 1). ADI 상세 없음 12.
  - `quality_status`: ok 683 · partial 8 · no_text 10(글자 없는 스캔본, OCR 안 함). `scope_status`: ADI part63_candidate 995 · out_of_scope 2,830 / Dashboard 132 · 104.
  - ADI와 Dashboard 사이 같은 파일(같은 sha256) 공유: 0건. 두 목록의 실제 중복은 파일 해시로는 안 잡힌다(다른 스캔·재입력본).
- 강화(11:51, **5초**): block 1,960(body 692 · signature 617 · question 237 · answer 234 · header 171 · condition 9) · cfr_reference 7,342(Part 63 5,181 · 60 1,608 · 61 297 · 62 251 · 기타 5) · document_relation 0 · facility_candidate 31(문서 23건).
  - 인용 해결: Part 63 5,140 연결, 미해결 2,202(Part 60·61·62 등 index 밖 2,161 + Part 63 41: `��63.67`처럼 §가 깨진 것, `63.12090`처럼 없는 조문, `Subpart DODOO` 오타).
  - 관계 0건: 철회·대체·같은 파일 문구가 든 블록은 116개(문서 101건)지만, 같은 줄에 Control Number까지 있는 줄은 3줄뿐이다. 그중 `Also filed as … M050002`(0500002)는 상대가 2005년(PDF 미수집)이라 건너뜀 1건, `Partially rescinds … M080027`는 M080027 자기 문서의 헤더(자기 언급 무시), `is rescinded. (See ADI Control Number M080027)`(M080028)는 문구와 번호가 다른 문장에 있어 규칙상 관계가 아니다.
- 적재(11:54, **14초**): release `5cc370d0-c513-4376-981f-2d910b39ae6b` published, `common_dataset_current(adi, adi+caa_dashboard)`가 가리킴. 원본 708개 등록(list 2 + letter 706). 표 9개 행 수 = jsonl. 검사 문제 0. `source_object_id`·`object_id` NULL 0.
- DB 용량 3,124 MB → 3,144 MB (+20 MB). `adi_*` 20 MB.
- 1차 적재 시도는 `adi_load` 열 중복으로 COPY 단계에서 실패(롤백, release는 staging으로 남음) → 고친 뒤 재실행이 같은 manifest_hash의 release를 재사용해 publish. release는 1개다.
- 자세한 것은 계획 문서 2-4·2-5·3-3·3-4.
