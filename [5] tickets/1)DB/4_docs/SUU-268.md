# SUU-268 docs(db): 적재 데이터 명세와 ODCS 데이터 계약 작성

## 목표
Supabase `public` 표 43개를 전수 조사해서, 신입용 명세 문서와 ODCS v3.1.0 YAML 데이터 계약을 만든다. 코드 동작은 바꾸지 않는다.

## 건드릴 파일
- `[1] docs/2) db/db overview/Data Specification_v1.md` (새로 씀)
- `[2] db/contracts/{ecfr,fr,echo,adi,rag,common}.odcs.yaml` (새로 만듦)

## 안 하는 것
- 로컬 raw/parsed 파일 조사
- CI에서 `datacontract test` 자동 검사 (다음 티켓)

## 완료 기준
- 문서·YAML의 표 수(43), 칸 수(430), 행 수가 2026-09-24 `count(*)` 결과와 같다
- YAML 6개가 ODCS v3.1.0 JSON Schema 검사를 오류 0개로 통과한다
