# SUU-114 chore(db): ADI·Dashboard 목록·상세·회신 PDF를 실제로 수집한다

## 목표
SUU-47~62에서 만든 수집 코드를 실제 네트워크로 한 번 돌려 `[2] db/4) ADI+CAA/*/raw/`(gitignore)를 채운다. 다음 티켓(평가셋 100건+ 확장)이 이 파일을 읽는다.

## 건드릴 파일
- 실행(Claude): `adi_collect.py`, `dashboard_collect.py`, `dashboard_letters_run.run`, `adi_details_run.run`, `adi_letters_collect.collect`(Part 63 후보만)
- 기록: 이 파일, `[1] docs/2) db/db 구축 계획/4_ADI+CAA 구축 계획.md` [1]-5

## 안 하는 것
- 코드 변경, 텍스트 추출 전수 실행, `adi_` 테이블 생성, 적재
- ADI 회신 PDF 3,825건 전부 받기 — 분당 20건이라 3시간 걸림. 상세의 Categories(MACT/NESHAP/GACT)로 Part 63 후보만 받는다. 나머지는 적재 티켓에서

## 완료 기준 ↔ 확인 방법
| 완료 기준 | 확인 방법 |
|---|---|
| ADI 목록·Dashboard 목록 run.json `succeeded` | 아래 결과 표 |
| Dashboard PDF 성공/실패 수 기록 | 아래 결과 표 |
| ADI 상세 3,825건 성공/실패 수 기록 | 아래 결과 표 |
| ADI Part 63 후보 PDF 성공/실패(이유별) 수 기록 | 아래 결과 표 |

## 결과 (2026-09-17)
| 단계 | 요청 | 성공 | 실패 | 비고 |
|---|---|---|---|---|
| ADI 목록 (`adi/raw`) | — | 3,825행 | — | 고유 Control Number 3,825, `succeeded` |
| Dashboard 목록 (`caa_dashboard/raw`) | — | 236행 | — | Part 63 명시 132행, `succeeded` |
| Dashboard PDF (`dashboard_letters/raw`) | 230 (고유 URL) | 229 | 1 | 404: Anchorage Regional Landfill 2020-10-20. 77MB, 5분 |
| ADI 상세 (`adi_details/raw/details.json`) | 3,825 | 3,813 | 0 | 100건씩 39요청, 90초. 12건은 상세 표에 안 나옴(요청 실패 아님) |
| ADI PDF (`adi_letters/shard_0..5/raw`) | 469 | 469 | 0 | 279MB, 6개 동시 8분 |

- ADI 상세 분류: NSPS 2,417 · MACT 762 · NESHAP 316 · Asbestos 270 · GACT 4. Part 63 후보 = MACT/NESHAP/GACT 또는 Abstract에 `63.x`/`Part 63` → **1,005건** (`adi_letters/part63_candidates.json`). Abstract가 비어 있는 후보 374건
- 후보 연도: 1990년대 이전 135, 1995~2004 401, **2005 이후 469**. PDF는 한 건 3MB·19초(스캔본)라 2005 이후만 받았다. 그 전 것은 스캔본이라 텍스트 추출도 안 될 가능성이 커서 평가셋에 못 쓴다. 나머지 536건은 적재 티켓에서
- ADI PDF는 `adi_letters/raw`(순차 실행 중단, 11건)와 `shard_0..5`(6개 병렬)에 나뉘어 있다. 적재 티켓은 폴더 6개를 합쳐 읽는다
- 수집 함수는 `ecfr_raw.save_raw`를 써서 `PYTHONPATH`에 `1_eCFR`이 필요하다. `adi_details_run`·`adi_letters_run`·`dashboard_letters_run`은 `__main__`이 없어 scratchpad 스크립트로 불렀다(코드 변경 없음)

## Codex 메모
Codex 작업 아님. 수집 함수는 `ecfr_raw.save_raw`를 쓰므로 `PYTHONPATH`에 `1_eCFR`을 넣어야 한다.
