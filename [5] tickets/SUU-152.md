# SUU-152 feat(rag): 답에 넣는 조문을 5개에서 10개로 늘려 102건 다시 잰다

## 목표
`run_answer.py`에 `--top-n`을 붙이고(기본 5), 상위 10조문으로 102건 새 run(~$2.5). A-5 ②.

## 건드릴 파일
- 고침: `[6] rag/eval/run_answer.py` — `top_sections(line, top_n=TOP_N)`, `--top-n` 인자, `fetch_texts`·`one()`·`answer_run_record(top_n=…)`가 인자를 따름
- 실행: `python "[6] rag/eval/run_answer.py" --top-n 10 --run-id 2026-09-19_answer_v2_gpt-5-mini_top10` → `results/…_top10.jsonl`, `answer_runs.jsonl` 한 줄 추가(3줄), `answer_failures.py --run-id …_top10`
- 문서: `[1] docs/3) rag/4_rag 답변 품질 개선 과정.md` 4절 A-5 ② 상태·5절 점수판 행 + 실패 분류·6절, `[6] rag/eval/rag_eval_plan.md` [14], `results/rag_eval_result.md` SUU-152 절
- 테스트(이미 있음, 수정 금지): `[6] rag/tests/test_run_answer.py` 끝 2개

## 안 하는 것
- 프롬프트(`ANSWER_SYSTEM`)·모델·심판 변경, 조문 20개, `MAX_COMPLETION_TOKENS` 변경
- 채점기·`answer_failures.py` 변경

## 완료 기준 ↔ 테스트
| 완료 기준 | 테스트 함수 |
|---|---|
| `top_sections(line, top_n=10)` → 앞 10개 | `test_top_sections_takes_top_n_when_given` |
| 인자 없으면 5 | `test_top_sections_defaults_to_five` |
| 새 run: 인용 Recall ≥ 0.74, Subpart·근거율 -0.03 이내, 심판 -0.06 이내 | 측정 → 문서, 사람 확인 |

## Codex 메모
- 비교 기준은 프롬프트 v2 run(SUU-151 채점): 0.941 / 0.707 / 0.998 / 1.686, 실패 9. 프롬프트는 이미 v2가 코드에 있으니 이번 run은 "v2 + top10".
- 미리 본 것: 상위 10에서 검색 Recall 0.733→0.804. `search` 3건 중 adi-M070020만 정답 조문이 들어옴(0/2→2/2). adi-M100018·M110005는 상위 10에도 없음. adi-M170004(2/5→4/5), adi-M150036(1/2→2/2)은 근거가 늘어남.
- 입력 토큰 ≈ 2배(15k→30k/건). 빈 답이 늘면 `--retry-empty`.
- `answer_runs.jsonl` notes: "SUU-152 상위 10조문(프롬프트 v2)".
