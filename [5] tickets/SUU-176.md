# SUU-176 feat(frontend): 히어로 제목은 은색 metal, 설명문은 파랗게 보인다

## 목표
h1 전체가 은색 metal 그라데이션 글자, 설명문이 accent-blue. 강조를 h1("AI Service")에서 설명문으로 옮긴다.

## 건드릴 파일
- 고침: `[4]frontend/src/app/globals.css` — `@utility text-metal` 추가
- 고침: `[4]frontend/src/app/page.tsx` — h1 `text-metal`, 파란 span 제거. p `text-accent-blue`
- 고침(Claude): `[4]frontend/tests/topnav_hero.test.tsx`, `tests/test_design_tokens.py`

## 안 하는 것
- 문구·크기·레이아웃·다른 요소

## 완료 기준 ↔ 테스트
| 완료 기준 | 테스트 함수 |
|---|---|
| globals.css `@utility text-metal`에 `background-clip: text` + `linear-gradient` | `test_globals_css_has_text_metal_utility` |
| h1에 `text-metal`, h1 안에 `.text-accent-blue` 없음 | `h1은 metal 질감이고 파란 강조는 없다` |
| 설명문 p에 `text-accent-blue` | `설명문은 accent-blue 다` |

## Codex 메모
- Claude가 설계와 구현을 같은 커밋에서 끝냈다.
- Tailwind v4 `@utility`로 정의해야 `text-metal`이 클래스로 먹는다. `color: transparent` + `background-clip: text`라 h1에서 `text-ink`는 뺐다.
