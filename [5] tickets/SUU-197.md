# SUU-197 fix(frontend): Ask 버튼 모서리를 질문 칸과 같은 둥근 네모로

## 목표
Ask 버튼이 옆의 textarea와 같은 `rounded-md` 모서리가 된다 (SUU-196에서 캡슐 모양이 된 `rounded-full`을 버림).

## 건드릴 파일
- `[4]frontend/src/app/applicability/page.tsx` — 버튼 `rounded-full` → `rounded-md`
- `[4]frontend/tests/framer_style.test.tsx` — "흰 알약" 검사를 `rounded-md` 검사로
- `[4]frontend/tests/ask_inline_form.test.tsx` — 버튼·textarea 모서리 클래스 동일 검사 1개 추가

## 안 하는 것
- TopNav·히어로의 알약 CTA는 그대로

## 완료 기준 ↔ 테스트
| 완료 기준 | 테스트 |
|---|---|
| 버튼 `rounded-md`, `rounded-full` 없음 | `framer_style.test.tsx` — Ask 버튼은 흰 둥근 네모다 |
| 버튼과 textarea의 `rounded-*` 클래스가 같다 | `ask_inline_form.test.tsx` — Ask 버튼의 모서리 클래스는 textarea와 같다 |
