# RAG 답변 품질 개선 과정

검색이 골라준 상위 조문을 LLM에 주고 **판정 기준표**(후보 Subpart, 적용 여부를 가르는 기준 + 근거 조문, 공장이 확인할 것)를 만들게 하고, 점수를 재면서 한 단계씩 고친 기록이다. 2026-09-19 ~. 검색 쪽은 `3_rag 검색 품질 개선 과정.md`, 계획은 `[6] rag/eval/rag_eval_plan.md` [14] A-행.

서비스 약속(`1_project.md`): **최종 판정은 주지 않는다.** "어디를 봐야 하고 무엇을 확인해야 하는지"를 준다. 답 모양도 채점표도 이 약속을 따른다.

## 1. 답 모양

입력 = 질문 + 검색 상위 조문 전문(기본 5개). 출력 = 아래 JSON 하나. 필드는 전부 필수.

| 필드 | 뜻 | 규칙 |
|---|---|---|
| `candidates[]` | 후보 Subpart | 1~3개. 넘겨준 조문에 나온 Subpart만 |
| `candidates[].subpart` | Subpart 코드 | 예 `PPPP` |
| `candidates[].title` | Subpart 이름 | |
| `candidates[].criteria[]` | 적용 여부를 가르는 기준 | 1개 이상. 한 문장, "~이면 적용 / ~이면 아님" 꼴 |
| `candidates[].criteria[].criterion` | 기준 문장 | |
| `candidates[].criteria[].citations[]` | 근거 조문 | 1개 이상. **넘겨준 조문 안에서만.** `40 CFR 63.xxxx(a)` 형식 |
| `checklist[]` | 공장이 확인할 것 | 1개 이상 |

없어야 하는 것: `applies`, `verdict`, `conclusion` 같은 최종 판정 필드. 기준을 주되 판정은 공장이 한다.

예시(평가셋 `dashboard-king-systems-2020-06-16`, 메틸렌클로라이드로 플라스틱을 녹여 붙이는 공정):

```json
{
  "candidates": [
    {
      "subpart": "PPPP",
      "title": "NESHAP for Surface Coating of Plastic Parts and Products",
      "criteria": [
        {
          "criterion": "플라스틱 부품 표면 코팅을 하는 HAP 주요 배출원(major source)이면 PPPP 대상이다.",
          "citations": ["40 CFR 63.4481(a)"]
        },
        {
          "criterion": "두 면을 붙이려고 바르는 물질은 '접착제'이고 접착제는 코팅에 든다. 용제만 발라 녹여 붙여도 코팅으로 볼 수 있다.",
          "citations": ["40 CFR 63.4581"]
        },
        {
          "criterion": "배출 한도는 코팅 고형분 1lb당 HAP lb로 정한다. 고형분이 전혀 없으면 한도를 계산할 수 없어 적용이 어렵다.",
          "citations": ["40 CFR 63.4490"]
        }
      ]
    }
  ],
  "checklist": [
    "바르는 물질에 고형분(수지·안료 등)이 조금이라도 들어 있는가",
    "접합 뒤 마른 막(dry film)이 남는가",
    "공장이 HAP 주요 배출원(major source)인가",
    "같은 라인에서 다른 코팅 작업도 하는가"
  ]
}
```

## 2. 채점표

평가셋 v2 102건(`rag_eval_case_v2.jsonl`). 각 케이스에 `gold_subparts`, `gold_citations`, EPA가 실제로 왜 그렇게 판단했는지 적힌 `notes`가 있다.

| 지표 | 무엇 | 계산 | 비용/102건 |
|---|---|---|---|
| Subpart 적중 | 답의 `candidates[].subpart`에 `gold_subparts[0]`이 있나 | 0/1, 케이스 평균 | $0 |
| 인용 Recall | `gold_citations`(조문 단위) 중 답의 `citations`에 나온 비율 | 케이스 평균 | $0 |
| 인용 근거율 | 답의 `citations`가 넘겨준 조문 안에 있는 비율. 밖이면 "지어냄" | 케이스 평균 | $0 |
| **판정 기준 점수**(주 지표) | gpt-5-mini 심판이 답의 `criteria`를 EPA `notes`와 비교 | 0·1·2점(0 틀림 / 1 부분 / 2 맞음), 케이스 평균 → 0~2 | ~$0.3 |

- 조문 단위 비교는 검색과 같다(`rag_eval_plan.md` [3]-1): `40 CFR 63.4481(a)(1)` → `section-63.4481`. 문단 번호는 뗀다.
- 심판 프롬프트에 들어가는 것: 질문, 답 JSON, `notes`. 심판은 "`notes`의 판단 근거가 `criteria`에 담겼는가"만 본다. 최종 결론 일치는 안 본다(답에 결론이 없으므로).
- 심판을 믿어도 되는지는 사람이 10건 읽어 확인한다(A-3, SUU-148). 일치율 70% 미만이면 심판 프롬프트를 고친다.
- 판정 기준 점수와 인용 근거율 중 근거율이 먼저다. 근거 없는 답은 점수가 높아도 못 쓴다.

## 3. 동점 폭

| 지표 | 동점 폭 | 왜 |
|---|---|---|
| 판정 기준 점수(0~2) | 0.06 미만 | 102건 중 3건이 한 단계(1점) 바뀌면 3/102 ≈ 0.03, 두 단계면 0.06. 검색의 "3건 = 0.03"과 같은 논리 |
| $0 지표 3개(0~1) | 0.03 미만 | 3건 차이 |

동점이면 싼 쪽·빠른 쪽을 고른다.

## 4. 실험 순서

한 번에 하나만 바꾸고 같은 채점표로 다시 잰다.

| 순서 | 무엇 | 티켓 | 상태 |
|---|---|---|---|
| A-0 | 답 모양·채점표 문서(이 문서) | SUU-145 | 완료 |
| A-1 | 채점기 코드 `ecfr_answer_score.py` | SUU-146 | 완료 |
| A-2 | 1차 측정 baseline: 상위 5조문 전문 → gpt-5-mini → 102건 채점 | SUU-147 | 완료 |
| A-3 | 심판 점수 10건 사람 검증 | SUU-148 | 예정 |
| A-4 | 실패 원인 분류(지어냄 / 검색 / 형식 / 답) + 채점기 subpart 표기 정규화 | SUU-149 | 완료 |
| A-5 ① | 프롬프트 v2: "Subpart는 코드만, 적용 가능한 subpart 다 적기(1~4)" | SUU-150 | 완료 — 동점(`format` 0으로 잡았지만 Subpart 0.912→0.922) |
| A-5 ②~④ | 조문 수 5→10→20(`search` 3), Subpart A(63.1~63.16) 항상 포함, Claude 비교 → 합격선 + CI | 후속 | 예정 |

## 5. 점수판 (v2 102건)

| 조합 | Subpart 적중 | 인용 Recall | 인용 근거율 | **판정 기준 점수**(0~2) | 실패 | 비용/102건 |
|---|---|---|---|---|---|---|
| baseline (SUU-147, 정규화 전 채점) | 0.863 | 0.685 | 1.000 | 1.667 | 15 | $1.45 (답 ~$1.2 + 심판 ~$0.25) |
| **baseline**: 상위 5조문 전문 → gpt-5-mini (SUU-149, subpart 표기 정규화 후 `--replay`) | **0.912** | 0.685 | 1.000 | **1.667** | 10 | $0 (같은 답을 다시 채점) |
| 프롬프트 v2: 코드만·다 적기 (SUU-150) | 0.922 | 0.707 | 0.998 | 1.686 (2점 72 / 1점 28 / 0점 2) | 11 | $1.38 |

- 심판 점수 분포: 2점 71건 / 1점 28건 / 0점 3건.
- 정규화 전 실패 15건 중 5건은 답이 `"Subpart M"`이라고 써서 채점기가 `M`과 다르다고 본 것. 채점기가 앞의 `Subpart` 단어를 떼고 대문자로 맞추게 고쳤다(SUU-149). 조문 번호(`40 CFR 63.460`)는 여전히 미적중. 지어낸 인용은 0건 — 프롬프트 "넘겨준 조문만"이 먹혔다.
- 인용 Recall 0.685는 검색 상위 5의 Recall@5(0.733)에 묶인다. 상위 5에 없는 정답 조문은 답에 못 나온다.
- 결과 파일: `[6] rag/eval/results/2026-09-19_answer_v2_gpt-5-mini.jsonl`, run 기록: `[6] rag/eval/answer_runs.jsonl`.
- 실패 분류(SUU-149, `python "[6] rag/eval/answer_failures.py"`): 실패 10건을 원인 하나씩으로 나눴다. 우선순위는 지어냄 > 검색 > 형식 > 답.

| 태그 | 뜻 | 건수 | case_id | 고칠 곳 |
|---|---|---|---|---|
| `fabricated` | 넘겨준 조문 밖 인용 | 0 | — | 프롬프트 |
| `search` | 정답 조문이 상위 5에 하나도 없음 | 3 | adi-M070020(G), adi-M100018(GGG), adi-M110005(MMMM) | 검색·조문 수 |
| `format` | 정답 조문은 줬는데 subpart 칸에 조문 번호를 씀 | 2 | adi-M070010(`40 CFR 63.460`), adi-M110015(`63.1065`) | 프롬프트·파싱 |
| `answer` | 정답 조문은 줬는데 Subpart를 잘못 골랐거나 심판 0점 | 5 | eaton-auburn(ZZZZ+PPPPP 중 PPPPP만, 심판 0), adi-M170004(DDDDD+HHH 중 HHH만), lhoist-montevallo(AAAAA 맞췄지만 심판 0), adi-M080034(FFFFF 대신 A·EE·YY), adi-M150036(AAAAAAA 대신 LLLLL·A) | 프롬프트·모델 |

- 프롬프트 v2(SUU-150) 실패 분류: 지어냄 1(adi-M180004, 63.8687 — 준 조문 바로 옆 번호) / 검색 3(같은 3건) / 형식 0 / 답 7. `format` 2건은 없어졌지만(adi-M070010 통과, adi-M110015는 subpart 4개를 적고도 CC를 빠뜨림 → `answer`), 새 실패 2건(adi-M050026 심판 2→0, adi-M180004 지어냄). 모든 지표가 동점 폭 안 → **채택하지 않고 기록만**. 후보 수 분포 1개 58→53건, 4~5개 0→11건: "다 적기"가 정답 둘째(ZZZZ, DDDDD)는 못 잡고 Subpart A만 덧붙였다.
- 실행 메모: 출력 한도 6000 토큰이면 102건 중 3건이 빈 답(추론 토큰이 다 먹음) → 10000으로 올리고 `--retry-empty`. 답 1건당 ≈ 15k 입력 토큰(조문 5개 전문), ≈ 5k 출력 토큰.

## 6. 배운 것

- 실패의 3분의 1(15건 중 5건)은 답이 아니라 채점기 문제였다. `"Subpart M"`을 `M`과 다르다고 봤다. 채점기를 먼저 의심하고, 점수를 올리기 전에 실패 케이스를 하나씩 눈으로 봐야 한다.
- 남은 실패 10건 중 7건(`format` 2 + `answer` 5)은 조문을 제대로 줬는데 모델이 subpart 칸을 조문 번호로 채우거나, 정답 Subpart가 둘일 때 하나만 골랐다. 검색이 아니라 프롬프트 문제 → A-5는 프롬프트 수정이 1순위, 조문 수 확대(`search` 3건)가 2순위.
- 지어낸 인용 0건. 인용 근거율 1.000은 이미 천장이라 A-5에서 지표로 안 쓴다. 대신 회귀 감시용으로만 둔다.
- (SUU-150) 프롬프트 한 줄로 `format`은 0이 됐지만 점수는 동점 폭 안. 형식 실패는 프롬프트로, 복수 정답 놓침은 프롬프트로 안 잡힌다 — 모델이 "둘 다"를 못 보는 게 아니라 정답 둘째 Subpart(ZZZZ, DDDDD)의 조문이 상위 5에 1~2개뿐이라 근거가 약하다. 다음은 조문 수 확대(A-5 ②)로 `search` 3건 + 복수 정답을 같이 본다. 같은 답이라도 재실행마다 심판 0점이 2~3건 사이에서 흔들린다(adi-M050026 2→0) → SUU-148 사람 검증이 필요하다.
