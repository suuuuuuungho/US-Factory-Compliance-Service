DB 구축 계획 Template.

- 각 데이터셋에대한 설명은 `C:\Users\Admin\Desktop\US Factory Compliance Service\[1] docs\2) db\db overview\Data Specification_v1.md`에 있다.
- 5살도 보고 이해할 수 있게 아주 쉽게 설명한다. 
- 간결하게 작성한다. 
- 아래 형식을 맞추되 너무 얽매이지는 않는다. 아래 형식을 뼈대 삼되 필요하면 새로운 카테고리를 형성해도 된다. 
- 표 형태로 작성하지 않는다. 넘버링하여 전개한다. 
- 넘버링 규칙. 넘버링할 때 들여쓰기 간격 일정하게 유지한다. 
  [1] -> 1) -> "-" 

1. Step.1 수집
1-1. 데이터 Overview
> guide: 반드시 전수 데이터를 살펴보고 데이터 수집 계획을 세운다. 각 데이터셋 수집 계획은 `C:\Users\Admin\Desktop\US Factory Compliance Service\[1] docs\2) db\db 구축 계획`에 데이터셋별 md 파일(`1_eCFR`~`4_ADI+CAA`)이 있다. 여기에 작성한다. 
> guide: 원본 데이터의 필드, 필드설명, 실제 값 예시를 적어주고, 채택한 필드는 별표 표시하고 그 이유를 적는다.
1-2. 수집 방법
> guide: 수집 출처와 경로를 남긴다. 
1-3. 수집 관련 유의점
1-4. 수집시 마주한 문제와 해결
1-5. 수집 결과
1-6. 수집 한계

2. Step.2 파서
2-1. 파서 방법
2-2. 파서 관련 유의점
2-3. 파서시 마주한 문제와 해결
2-4. 파서 합격 기준과 파서 실행 결과
2-4. 파서 결과
2-5. 파서 한계

3. Step.3 DB 적재
3-1. DB 적재 방법
3-2. DB 적재 관련 유의점
3-3. DB 적재 시 마주한 문제와 해결
3-4. DB 적재 실행 결과
3-5. 테이블 스키마 
> guide: 필드, 필드 설명, 실제 값 예시, 다른 테이블 필드와의 관계 연결

4. Step.4 갱신되는 데이터 자동 적재
4-1. DB 적재 방법
4-2. DB 적재 관련 유의점
4-3. DB 적재 시 마주한 문제와 해결
4-4. DB 적재 실행 결과

